# Findings

## 2026-06-28 (补充 - 树分支视觉与项目树点击兜底)

- 层级关系最好通过主干线、分支线和缩进共同表达，单纯的卡片堆叠会让“客户 -> 项目 -> 岗位”的关系不够直观。
- 项目页展开箭头如果只依赖全局点击分发，容易出现局部事件流漏接；给按钮补直接调用的兜底可以明显提高稳定性。

## 2026-06-28 (补充 - 编辑上下级固定展示与项目树展开)

- 项目编辑和岗位编辑的上级关系是确定性的上下文信息，应该直接展示当前客户名或客户名 + 项目名，而不是允许用户重新选择。
- 项目管理页的展开箭头需要和客户页一样走统一树状态处理，才能保证项目节点可展开到岗位层。

## 2026-06-28 (补充 - 客户/项目树状总览补强)

- 项目编辑弹窗在树状展开场景下需要额外回显当前客户名称，否则只展示客户下拉框会让编辑上下文不完整。
- 项目管理页也适合直接承载“项目 -> 岗位”展开浏览，和客户页保持同款树状交互，减少用户在多个标签页之间来回切换。

## 2026-06-28 (补充 - 客户树状总览页)

- 客户管理页现在适合承载“客户 -> 项目 -> 岗位”的单页总览入口，最稳妥的做法是保留项目页/岗位页作为独立工作台，同时在客户页增加树状展开视图。
- 树状总览需要按需加载子节点，首屏只拉客户数据，项目和岗位在展开时再请求，这样更符合原型里“先看客户，再钻取项目/岗位”的使用方式，也能避免首屏一次性拉全量岗位。

## 2026-06-25 (补充 - 异步按钮即时反馈)

- 当前异步按钮主要集中在 `app.js` 的全局 `button[data-action]` 委托，简历导出、AI 检索、日志/通知/AI 任务刷新、候选人保存、推荐保存等高频动作都从这里进入。
- 此前很多按钮只在异步操作完成后通过 Toast 或页面刷新反馈，用户点击后到接口返回前缺少可见状态，容易误判为“没点上”。
- 已新增共享 `withButtonBusy()` 机制：按钮进入异步流程后会立即显示 spinner + 忙碌文案、设置 `aria-busy=true` 并禁用，异步结束后恢复原始 HTML 和禁用状态。
- 简历导出弹窗打开和勾选导出会先写入“正在加载...”空态；确认导出时按钮会显示 `导出中...`，批量循环中会更新为 `导出中 N/M...`。
- Playwright 延迟接口验证已确认 `export-selected`、`confirm-export-upload`、`confirm-ai-search`、`refresh-audit-logs` 点击后 80ms 内进入忙碌态。

## 2026-06-25 (补充 - 权限系统登录与横切数据权限闭环)

- 权限系统此前已有 RBAC 和部分数据范围过滤，但登录仍是开发态：前端默认 `dev-token`，后端登录固定校验 `admin123` 并返回同一个管理员 token，leader/operator 无法形成真实登录身份。
- 本次已改成按 `users.password_hash` 校验登录，签发 `user:<username>` 身份 token，并在退出、登录、用户密码重置等关键动作写入审计日志。
- `/api/me` 现在会返回当前角色启用的功能权限，前端侧边栏优先使用 `role_permissions` 控制菜单可见性，角色硬编码只保留为缺少权限记录时的兜底。
- 默认账号口径收口为 `admin/admin123`、`leader/leader123`、`operator/operator123`；seed 和测试清理会把默认账号恢复到该口径，避免测试或部署环境被旧 `dev` 密码污染。
- 数据权限不再只覆盖候选人列表；推荐、反馈、交付、导出、统计、通知和 AI 任务已补同源过滤，普通用户不能通过这些接口绕过列表权限。
- 工作区仍存在 `chrome_dev_profile` 自动生成改动，属于无关浏览器缓存，不应纳入权限系统提交。

## 2026-06-25 (补充 - 团队归属是组权限的主逻辑)

- 用户确认组权限的核心业务逻辑是“组长看自己和下属组员数据，组员只看自己数据，不同组长之间互相隔离”。
- 为支持该逻辑，权限模型新增 `users.manager_user_id` 表示直属组长，客户、项目、岗位新增 `owner_user_id` 表示业务数据归属人。
- 数据权限判断现在优先支持团队归属：操作员看自己 owner 的数据；组长看自己和直属下属 owner 的数据；超级管理员全量可见；原 `company/project/position` 数据权限仍作为额外授权能力保留。
- 用户管理页已提供“直属组长ID”字段，后台会校验直属上级必须是组长。

## 2026-06-25 (补充 - AI 检索只存在于本地未提交改动)

- 当前仓库 `main` 分支与 `origin/main` 处于同一个提交 `7e87577`，没有本地领先或落后的已提交 commit。
- 但工作区是脏的，`src/pages/candidates.html` 和 `app.js` 都有未提交修改，其中就包含 `AI检索` 按钮、`data-ai-search-modal` 弹窗和 `open-ai-search-modal` / `confirm-ai-search` 事件处理。
- `origin/main` 上的对应文件不包含这些 AI 搜索代码，所以远程即使已经 `git pull` 到最新提交，也不会看到这部分功能。
- 这次差异的根因不是“远程没 pull 成功”，而是“功能停留在本地工作区，尚未 commit/push”。

## 2026-06-25 (补充 - root 8000 now lands on homepage)

- The server is already exposing the static site through port 8000; the missing piece was a root `index.html` at the repository root.
- Adding a root `index.html` with a redirect to `/src/pages/dashboard.html` makes `http://<host>:8000/` open the app directly.
- This is the simplest fix because it works with the existing `StaticFiles(directory=ROOT_DIR, html=True)` mount without changing backend routing behavior.

## 2026-06-25 (补充 - Windows scripts localized to English)

- The Windows deployment scripts were causing console encoding noise because they mixed Chinese prompts with Windows terminals.
- All Windows-facing batch scripts and the deployment text guide now use English-only prompts and instructions.
- The backend table initializer also prints English output now, so both command prompts and log windows stay readable on Windows.

## 2026-06-24 (补充 - Windows 逐步部署脚本)

- 用户明确不需要一键脚本，所以部署入口现在改成“安装依赖 / 初始化表结构 / 启动后端”三步拆分，更适合现场逐步排查。
- `windows/01-install-deps.bat` 负责依赖安装，`windows/02-init-db.bat` 只建 `public + recruit` 表结构，`windows/03-start-backend.bat` 只启动服务。
- 根目录 `Windows部署说明.txt` 已同步更新为逐步执行方案，方便 Windows 机器按顺序照着部署。

## 2026-06-24 (补充 - .env 统一数据库配置)

- 数据库连接串现在和 OpenRouter 一样统一放在仓库根目录 `.env`，不会再只靠启动脚本里的环境变量临时设置。
- `backend/app/config.py` 已改成先加载 `.env` 再实例化 `Settings`，否则 `DATABASE_URL` 会在导入时被默认值抢先固定。
- 这意味着 Windows 下只要 `.env` 存在并写好 `DATABASE_URL`，`run-windows.*` 和 `init-db-windows.*` 都能直接读到同一份配置。

## 2026-06-24 (补充 - PostgreSQL 连接串与 Windows 一键建表)

- PostgreSQL 连接串的主入口是 [backend/app/config.py](/Users/huaiyuan/Desktop/workspace/hr-plateform/backend/app/config.py) 的 `DATABASE_URL`，同时 `backend/app/main.py` 会加载仓库根目录 `.env`，所以 Windows 上可以用环境变量或 `.env` 覆盖默认值。
- 默认连接串目前统一指向 `postgresql://user_delivery:delivery_pass@localhost:5432/hr_platform`，和现有 `run-windows.*` 保持一致。
- 已新增只建表不导数据的 Windows 入口 `[init-db-windows.bat](/Users/huaiyuan/Desktop/workspace/hr-plateform/init-db-windows.bat)` / `[init-db-windows.ps1](/Users/huaiyuan/Desktop/workspace/hr-plateform/init-db-windows.ps1)`，底层调用 `backend/scripts/init_tables.py`，复用 `ensure_schema()` 来创建 `public` + `recruit` 表结构。

## 2026-06-24 (补充 - Windows 启动脚本落地)

- 这个项目不需要为了 Windows 重写业务代码，当前主要差异在启动层和 shell 脚本层。
- 已补出可直接在 Windows 上使用的 `run-windows.bat` 和 `run-windows.ps1`，默认使用 PostgreSQL 的 `DATABASE_URL` 并启动 `backend.app.main:app`。
- 现阶段只要 Windows 上装好 Python、PostgreSQL 和 Playwright，就可以用这两个入口启动项目；后续若有需要，再补测试/收尾脚本的 Windows 版本。

## 2026-06-24 (补充 - AI 检索加载提示与接口联调)

- `AI检索` 已接成一个真正的搜索模式，不是模拟弹窗；它只在当前筛选后的候选池里做匹配，并返回单条候选人记录。
- 点击搜索后先展示 `AI深度匹配中...` 的加载提示，接口返回并刷新列表后再关闭，交互上能明确感知“正在分析”和“已出结果”两个阶段。
- 后端 `create_candidate()` 需要显式排除 `record_key`，因为它是聚合列表/检索层字段，不是 ORM `Candidate` 的持久化列。
- 当前 AI 搜索测试已改为接口级稳定桩，避免被 OpenRouter 可用性或 fallback 逻辑干扰。

## 2026-06-24 (补充 - 薪资记录岗位联动改造)

- 薪资/福利/入职条件记录现在新增了 `position_id` 作为真实关联源，前端只允许从岗位管理里选择岗位，不再手输岗位名。
- `company_name` 不再由用户输入，而是由 `position.project.company.name` 反查带出，保存时仍回填到薪资记录里，保证详情页和入职状态面板继续沿用现有字符串展示。
- 这次联动已经用回归测试验证：新增记录会同时写入 `position_id`、`position_name` 和 `company_name`，编辑记录切换到另一个岗位后，这三个字段会一起更新。

## 2026-06-24 (补充 - 候选人池同名重复与详情空白)

- 详情页没显示解析字段，不是详情接口没读到，而是候选人池列表里同时暴露了“旧下载占位行”和“已落库的 AI 解析行”，用户很容易点到前者。
- 当前 `candidates` 查询结果里，`焦光瑜` 只有一条真正的 AI 解析候选人记录，但旧的 `recruit.resume_downloads` 里还有多条同名下载记录。
- `list_candidates` 原本只在“纯下载行”之间按姓名去重，没有在“已经存在落库候选人”时压掉同名旧下载行，所以同一人会在列表里看起来像两条。
- 已改成：只要同名候选人已经存在落库记录，就不再展示对应的旧下载占位行；现在 `焦光瑜` 的搜索结果已收敛为 1 条，且带完整解析字段。

## 2026-06-24 (补充 - 候选人重复行的真实成因)

- 这次看到的重复行，不是“期望职位”在分组，而是同名简历被写成了不同的业务记录：`list_candidates` 以 `candidate_agent_id` / `download.id` 作为来源关联键，当前这几条 `焦光瑜` 的 `candidate_agent_id` 不同，所以会被当成不同记录展示。
- `record_key` 已经把列表行区分开，但列表层目前仍是“按来源记录展示”，不是“按姓名强行折叠”。如果要改成“一人一条”或“同一 PDF 只显示一条”，需要再定义更强的合并规则，比如按文件指纹、稳定 source id 或最新解析结果归并。

## 2026-06-24 (补充 - 候选人列表按同一简历去重完成)

- 候选人列表已改为按简历文件指纹去重：同一份 PDF 只保留一条 canonical 记录，优先展示字段最完整、且本地已落库的候选人主记录。
- 对没有文件路径锚点的纯手工候选人，仍然保留独立行，不会被误合并。
- 复测 `焦光瑜` 搜索结果已从 3 条收敛为 1 条，当前只显示 `candidate:1` 这条完整记录。

## 2026-06-24 (补充 - 候选人主键收口为 candidate_agent_id)

- 去重规则已从“文件指纹”收口为“`candidate_agent_id` 主键优先”。
- 同一 `candidate_agent_id` 下，列表只保留 `candidate` 主表记录；`recruit.resume_downloads` 仅在没有对应主表时兜底展示。
- 纯手工候选人（没有 `candidate_agent_id`）继续按自己的主键独立保留，不参与这层合并。

## 2026-06-24 (补充 - 冗余候选人记录清理)

- 已删除信息更少的冗余候选人主记录 `candidates.id = 10160`，保留信息更完整的 `candidate:1`。
- 当前看到的同名剩余记录来自 `recruit.resume_downloads`，不是 `candidates` 主表重复行。

## 2026-06-24 (补充 - 单条简历分析已恢复)

- `candidates` 表确实被大范围清空过，当前只剩少量记录；这次不是“字段没读出来”，而是数据本身被删掉了。
- `recruit` 抓取表里还能回灌的只是抓取侧来源，不是这次用户要的 AI 简历解析结果；用户确认后，改为直接从原始 PDF 重新跑简历解析。
- 已成功把候选人 `焦光瑜` 这一条重新解析并写回 `candidates`，用于演示的 AI 简历分析已恢复。
- 这次只恢复了一条样本，不是批量恢复；若后续要补回更多真人候选人，需要继续逐条重跑 PDF 解析，或者提供数据库备份再做全量回滚。

## 2026-06-24 (补充 - 统计管理 summary 缺字段导致 undefined)

- `src/pages/statistics.html` 的数值展示全部来自 `/api/analytics/summary`，不是前端写死的业务数据；前端只负责把接口返回值拼到卡片和排行面板里。
- 之前 `/api/analytics/summary` 只返回了候选人、推荐、交付、评价、通知、标签、质保和排行，没有返回 `company_count / project_count / position_count / user_count / audit_log_count`，因此页面里对应位置渲染成了 `undefined`。
- 已改成先复用 `crud.dashboard_summary(db)` 取基础数据库统计，再补上评价、通知、标签、质保和排行字段；现在这些卡片会直接显示数据库里的真实数字。

## 2026-06-24 (补充 - 评价记录保存后展示口径不一致)

- 评价体系页面的首屏列表模板本来就会按 `候选人名 / 岗位名 / 轮次 / 备注` 渲染，但 `app.js` 在 `confirm-evaluation-upload` 后手动刷新列表时，使用了另一套更老的模板。
- 那套旧模板优先展示 `evaluator` 和 `岗位 ID`，所以保存后会把页面首屏的展示口径覆盖掉，看起来像“没有显示新增评价里填的信息”。
- 已把保存后的刷新逻辑改成和首屏一致，统一展示候选人、岗位、轮次、等级、分数与备注内容。

## 2026-06-24 (补充 - 评价体系新增评价 500)

- `POST /api/evaluations` 之前是直接创建 `Evaluation`，没有像候选人跟踪、薪资记录等接口那样先调用 `ensure_local_candidate`。
- 当下拉里选中的候选人只存在于候选人聚合列表、但还没有本地 `candidates` 落库时，数据库会在插入 `evaluations.candidate_id` 时触发外键约束，最终表现为 500。
- 已把评价创建接口改成先补齐候选人，再校验岗位是否存在，缺失时返回 404，而不是把约束错误直接暴露成 500。
- 用同样的请求复测后，`POST /api/evaluations` 已能正常返回 200；为了避免测试污染，刚创建的验证记录已同步删除。

## 2026-06-24 (补充 - 测试数据统一清理脚本)

- 新增 `scripts/cleanup_test_data.py` 作为一键清理入口，默认执行全量重置并恢复种子行，支持 `--dry-run` 先看会影响多少表。
- 将 pytest 收尾逻辑抽到 `backend/test_cleanup.py`，`tests/conftest.py` 与手工清理脚本现在复用同一套规则。
- `resume_parse_tasks` 之前漏进了清理顺序，导致测试结束后残留 91 条；已在初始化基线阶段清零，并验证清理后该表回到 0。
- `recruit.*` 外部表当前用户没有删除权限，因此不纳入默认清理目标，避免脚本在权限不足时失败。

## 2026-06-24 (补充 - 客户管理页面与删除链路修复)

- `customers.html` 的展示数据来自 `window.hrApi.companies()/projects()/deliveries()`，不是前端硬编码；页面初始空态只是占位，实际列表由 API 回填。
- 客户删除 500 的根因是 PostgreSQL 外键链路未显式清理，`companies -> projects -> positions -> recommendations -> {deliveries, recommendation_feedbacks, candidate_tracking_events}` 以及 `evaluations` 都会参与约束。
- 已把公司删除和项目删除改为“先删下游再删上游”，并补了回归测试，当前删除带推荐/反馈/交付/跟踪记录的客户可以正常返回 200。
- `backend/app/main.py` 的 OpenAI client 改成懒加载创建，避免 import 阶段被 `httpx` 版本兼容性卡住，pytest 现在可以正常导入应用。

## 2026-06-24 (补充 - 空 candidate_agent_id 候选人清理)

- 已清理 `candidates` 表中全部 `candidate_agent_id` 为空的历史测试候选人，共 9 条。
- 清理时一并删除了这些候选人关联的推荐、反馈、交付、薪资、入职、随访、邮件、备注、导出记录和物理导出文件，避免留下孤儿数据。
- 当前数据库中空 `candidate_agent_id` 候选人数量已归零。

## 2026-06-24 (补充 - 侧边栏结构收口)

- 侧边栏已收口为 PRD 里的 `主要功能` / `系统设置` 两个主分组，菜单顺序与命名按 PRD 对齐。
- `数据探针` 按要求单独保留在 `开发工具` 分组，不并入 PRD 两大分组。
- 本次只收口菜单与分组结构，未删除后端表结构或迁移，避免影响 `positions` 等共享业务链路。

## 2026-06-24 (补充 - PostgreSQL 口径收口)

- 当前可见的旧数据库文案和旧迁移入口已改写为 PostgreSQL 口径，仓库默认叙事已经切换到 PostgreSQL。
- `backend/migrate_to_pg.py` 已改为 PostgreSQL 到 PostgreSQL 的同步工具，不再承载旧数据库迁移说明。

## 2026-06-24 (最新 - 全局测试数据清理与基线回写)

### 测试后的数据库收口策略
- 新增 `tests/conftest.py` 的 `autouse` 清理 fixture 后，所有会写数据库的回归测试都会在 teardown 自动删除本轮新增记录，避免测试数据持续累积。
- 对测试会改写的基线配置表做了固定回写：`User(admin)`、`WarrantyRule`、`SystemConfig`、`EmailConfig` 在每轮测试后都恢复为默认值，保证 `test_employment_onboarding.py` 和系统配置类烟测不会互相污染。
- 已清理当前库里的明显测试残留标记行，包括 `temp_%` 用户、`TEMP_/DEL_` 角色、`page:permissions:smoke` 角色权限、`team-smoke` 数据权限、`新通知` 通知、以及测试 AI 任务，确认当前最新烟测结束后这些残留不会继续增加。
- 目前清理策略主要覆盖 public schema 的业务表；`recruit` schema 表由于当前运行权限边界未纳入本轮自动删除，但现有测试链路没有依赖这些表做写后收尾。

## 2026-06-23 (最新 - 候选人入职状态双模联动与质保校验)

### 质保期规则表配置与计算自愈
- **质保字典表（WarrantyRule）的重用设计**：我们避免了在数据库中创建冗余的“入职质保期”新表，而是直接重用了现有的 `WarrantyRule` 表。在 `seed.py` 中为 `scope="入职质保期"` 配置了 `months=2`（代表 60 天质保期）的种子数据，保证各业务数据源和校验体系的强一致性。
- **日期差异计算逻辑修正**：前端在计算已录入候选人是否“质保在职”时，通过 `new Date() - new Date(onboard_date)` 计算天数差异，移除了 Math.abs 带来的绝对值转换，从而能够更严密地处理未来时间入职的情况（即未来的入职时间计算结果为负天数，也被正确包含在 60 天质保期范围内并展示“质保在职”），提高了商业逻辑的严密性。

### 面试跟踪与候选人大状态后端联动
- **后端接口级联更新**：在 `POST /api/employment-records` 接口中，系统在新增或更新入职记录的同时，自动通过 `order_by(created_at.desc()).first()` 查出该候选人最新的一条面试记录（`CandidateTrackingEvent`），并同步更新其入职状态字段（`employment_status`）。同时，将候选人本身的 `status` 变更为 `"已录用"`（已入职）或 `"未锁定"`（未入职），实现了底层数据大状态的全生命周期同步。

### 页面生命周期的自动化重新渲染
- **避免硬编码拼接**：以往在保存后会使用局部 HTML 片段拼接，由于历史记录已经存在“入职”标记而经常导致状态更新失败。我们重构了保存事件回调，直接通过调用公共的 `window.fetchCandidatePanels(candidateId, container)` 局部重新拉取并刷新候选人生命周期，以保证列表、状态与数据库在操作后时刻完全一致。
- **双模卡片美化**：在生命周期列表中，对于“已入职”显示绿色成功的“入职”微标并展示公司、岗位；对于“未入职”则显示灰色的“入职”微标并展示未入职的备注原因。

### 候选人详情弹窗一站式无缝嵌入整合
- **取消独立 Modal 弹窗模式**：响应用户对“最下面依旧只看到薪资福利跟踪表，没有入职卡片”的体验反馈，我们将原有的独立 `data-candidate-employment-modal` 弹窗彻底删除，将其作为常驻的 Panel 组件直接追加到候选人详情弹窗（`data-candidate-detail-modal`）的最下方（紧跟在薪资/福利/入职条件跟踪表之后）。
- **详情打开时自动初始化**：在 `view-detail` 事件监听最后，自动并发加载并渲染入职状态所需的最新薪资记录、已有入职记录、以及质保天数限制，实现了无需二次点击的一站式状态查看。
- **编辑至详情的平滑过渡**：当用户在“编辑资料”或“随访记录”弹窗里点击“入职状态”时，脚本自动静默关闭编辑弹窗、调起详情弹窗、并在渲染完毕后自动平滑滚动定位到最下方的“入职状态”区域，做到了顺滑无断层的交互导流。
- **开关滑动即时生效（无需确认按钮）**：
  - **已入职模式**：将 Toggle 键滑到“已入职”时，不再保留多余的确认入职按钮。系统在通过约定薪资防呆校验后，**直接在滑动那一刻异步提交保存请求并生效**，同步联动重刷详情及生命周期卡片。
  - **未入职模式（编辑与已确认双态展示）**：滑到“未入职”时，由于需要填写并确认原因，展现红框内的“输入编辑态”；一旦用户点击“确认未入职”提交成功后，该编辑界面（textarea 与确认按钮）自动隐退，直接以精致的文字提示展现已确认保存的备注内容，避免了表单重复修改的视觉混乱。若打开详情时已有备注记录，则默认直接渲染已确认展示态。

## 2026-06-23 (最新 - 薪资福利入职跟踪表集成与客户公司模糊搜索)

### 面试轮次关联与只读控制校验
- **只读置灰机制**：根据约束，如果薪资记录在数据库中已经记录了轮次，则直接使用且不允许修改。我们在前端编辑时，通过将 `<select id="salary-interview-round">` 设为 `disabled = true` 阻止用户修改。在后端接口 `PATCH /api/salary-records/{record_id}` 中同步添加了业务检查：如果已存的轮次非空，则丢弃任何对它的变更载荷，维持原数据库值，实现了端到端的防篡改校验。

### 多记录无阻塞存储（废除 Upsert）
- **数据关联性解耦**：移除旧版单候选人薪资 Upsert 逻辑，支持添加多条薪资/福利/入职条件跟踪记录。每次提交新建请求将调用 `POST /api/salary-records` 创建全新行。在前端表格的操作列提供蓝色编辑（铅笔图标）和红色删除（垃圾桶图标）并列小按钮，并挂接 `DELETE /api/salary-records/{record_id}` 接口进行物理删除。

### 基于 HTML5 Datalist 的客户公司模糊搜索及选择
- **零依赖级联支持**：将“客户公司”输入框升级为 `<datalist id="salary-companies-list">` 绑定的模糊搜索组件（符合 KISS 原则）。在添加或编辑弹窗打开时，前端自动异步请求 `window.hrApi.companies()` 列表填充 Datalist。用户既可以直接下拉选择已有公司，又可以在输入框内打字按名称过滤匹配。

### 动态登录操作员注入
- **当前登录用户捕获**：前端 `app.js` 在登录加载完毕后，将当前登录用户信息缓存在 `window.currentUser` 中。在保存薪资记录时，前端自动提取当前登录者的姓名（`full_name`）或账号（`username`），不再写死 `"张三"`，并在获取失败时降级回退至 `"管理员"`。

## 2026-06-22 (搜索工具栏精简与期望城市省市级联选择器实现)

### 二级级联城市选择器性能与交互
- **纯前端检索优势**：由于页面已通过 `window.candidatesPageState.rawList` 缓存候选人，省市数据字典使用纯前端的 JS 静态常量 `CITY_DATA`（包含 10 个省份及其代表城市）。输入检索和级联点击时，完全在前端由 DOM 响应并更新输入框，再触发底层的 `applyFilters` 在内存进行二次过滤。无需额外发起 HTTP 请求，大大提高了界面响应的即时性和交互的顺滑感。
- **级联面板点击防呆**：对于通过 input 的 `focus` 触发的下拉浮层，需要同时监听全文档的 click 事件。如果用户点击了输入框或下拉弹层内部，则保持显示；如果点击在输入框和弹框包裹器 `city-selector-wrapper` 外部，则必须将 display 设为 `none`，以避免浮层常驻而遮挡其他下拉选项或列表内容。

### 导出选中候选人的过滤条件同步
- **基于 data-id 收集选中状态**：将【导出简历】按钮迁移至候选人列表标题右侧。当点击导出按钮时，脚本通过 `.table-card .table-row input[type="checkbox"]:checked` 收集已被勾选的元素，并映射出它们的 `data-id`。
- **预设参数约束**：如果勾选为空，应直接利用 Toast 拦截提醒，不再调起导出弹窗；如果有勾选，则获取全部候选人数据后，使用 `candidates.filter(c => checkedIds.includes(c.id))` 过滤掉所有未被勾选的行，并使用这一精炼列表来拼装导出弹框的“候选人”选择下拉列表（`<select data-export-candidate>`），这极大地减少了操作失误，也方便了用户快速进行单人/目标人选的水印导出。

## 2026-06-22 (PostgreSQL 迁移与隔离完成)

### PostgreSQL 权限隔离与架构原则
- **Schema 划分与用户细粒度控制**：
  - **主站交付表** (25张) 默认位于 `public` schema 下，由后端使用的 `user_delivery` 角色拥有全量读写及关联 Sequence 自增权限。
  - **Recruit 抓取表** (5张) 在 PostgreSQL 模式下被划分至独立的 `recruit` schema。
  - 后端账号 `user_delivery` 对 `recruit` schema 仅拥有 `USAGE` 与 `SELECT`（只读查询）特权，任何对抓取表的数据写操作都会触发 `InsufficientPrivilege` 数据库异常，实现了强物理级别的数据安全隔离。
- **SQLAlchemy 跨 Schema 反射元数据规则**：
  - 在 PostgreSQL 多 Schema 架构下，使用 `inspect(engine).get_columns(table_name)` 时，默认只会检索 `public` schema。
  - 对于划分到非默认 schema（如 `recruit`）的表，如果不显式传递 `schema="recruit"` 命名空间参数，反射机制将无法获取其列元数据（返回空列表），进而破坏动态行拼装逻辑。
  - 在设计反射与数据探针等高度动态的元数据分析接口时，系统必须为被隔离在非默认命名空间下的表配置动态 schema 映射。
- **动态方言支持**：
  - 系统在 Python ORM 层判断当前 `database_url` 是否属于 PostgreSQL 兼容连接。如果是 PostgreSQL，则应用 schema 绑定，确保表结构按需分配。
- **防范 sequence 跳变破坏硬编码 ID 的测试与 Seed 约束**：
  - 在 PostgreSQL 中，因为事务 rollback 导致 primary key 的自增序列发生跳变，所以在数据 seeding 与回归测试（`test_phase1_smoke.py`, `test_phase2_smoke.py` 等）中**绝对不要硬编码 ID 1 进行依赖级联插入**。
  - 所有需要级联引用的种子数据和测试对象，必须在插入后调用 `db.flush()` 动态取得最新自增 ID，或通过 API/SQL 动态反查（如根据 `username='admin'` / `name='张三'` 获取实际 ID）来进行下游记录插入，确保在任何测试环境下脚本的完全幂等与健壮性。

## 2026-06-22 (简历池主键映射机制与表单实时校验)

### 候选人扩展字段联查漏洞与修复
- **发现**：后端 `list_candidates` 接口进行 `Candidate` 和 `RecruitCandidateProfile` `LEFT JOIN` 查询时，采用了在代码中手动构建返回 `dict` 的方式进行虚实数据整合。这导致近期在 `Candidate` 模型新增的十几个扩展字段（如户口、出生日期、职位状态等）即便成功存入 PostgreSQL 数据库，但在前端重新拉取列表时仍因字典中未包含这些键而无法显示。
- **修复**：已在 `crud.py` 的手动构建字典逻辑中全面补充所有的简历扩展字段，确保前后端数据流转完整无缺。

### 简历池主键映射设计核心结论
- **逻辑关联与主键绑定**：已验证 `ensure_local_candidate` 拦截器不仅通过 `candidate_agent_id` 反查抓取表获取补充信息，且在初始化交付表记录时，**直接使用 `resume_downloads.id` 作为 `Candidate.id` 赋值**，实现了交付表主键与抓取表唯一数值 ID 的绝对绑定。这从根本上消灭了基于字符串操作带来的各类强转 `NaN` 问题。
- **自增序列号隔离机制**：针对 `public.Candidate`，PostgreSQL 中其对应的序列 `candidates_id_seq` 已被设定为从 `10000` 起始（当前已达 `10019`）。这通过数字分段机制，巧妙隔离了“本地直接手工新增候选人（id>10000）”与“来自智联抓取池的虚拟映射候选人（id<10000）”，彻底杜绝了手动添加可能导致的主键冲突风险。

### 实时表单合规校验
- 优化了用户交互体验，在 `app.js` 的全局级别增设 `focusout` 事件监听，对“手机号”、“身份证”、“邮箱”三个字段进行实时正则表达式校验。
- 采用即时报错机制：校验失败时立刻通过 Toast 提示用户，并标红当前输入框边框，修正后边框恢复，从而避免填完全表点击保存时才集中报错的负面体验。

## 2026-06-23 (最新)

### 数据库资源探针物理表动态反射与免过滤设计
- **完全解耦硬编码白名单**：移除以前前端和后端写死的 `allowed_tables` 白名单，将数据库资源探针改为通过 SQLAlchemy `inspect(db.bind)` 在运行时动态发现全部 Schema 下物理业务表名（跳过 `alembic_version` 迁移控制表），并按字母排序返回。这保证了任何新加的物理表（例如新入库的 `candidate_notes`）在被创建后无需任何代码修改即可自动在数据探针左侧列表中显示。
- **非映射物理表 Native SQL Fallback 查询支持**：在获取表数据时，如果目标物理表在后端 `model_mapping` 中未定义 ORM 模型（如新表或第三方临时表），接口能够动态使用 `inspector.get_columns` 发现其字段结构，并 Fallback 使用原生 SQL 发起 `SELECT *` 查询。这为探针提供了系统级的物理层透视能力，对非映射表也能稳定实现字段表头及行数据加载，确保 100% 无死角过滤呈现。

### 候选人跟踪表（面试记录）行内快捷修改与删除
- **高复用弹窗双模切换 (KISS)**：对于面试记录的编辑，我们成功复用了原先用于“新增”的 `data-add-tracking-modal`。在点击行内“编辑”铅笔图标时，弹窗自动读取事件数据、回填历史表单信息并将其状态 `mode` 设为 `edit`，标题改写为“编辑面试记录”。保存时再行进行 API 双向分发（`PUT` / `POST`），免去了在 HTML 模板中创建新 Modal 的冗余。
- **行内纯图标排版优化**：为缩减操作列占用空间并提升 UI 美观度，我们将发送邮件按钮去除了“发送邮件”四个汉字与前置 emoji，将其缩小为 24px 高颜值的圆圈青色 SVG 邮件信封图标按钮。并以 `gap: 6px;` 水平并列展示了蓝色编辑铅笔图标、红色删除垃圾桶图标，使整行交互极为紧凑直观。

### 候选人跟踪表（面试记录）字段升级与自愈式迁移
- **表结构精细化扩展**：根据产品高保真截图设计，对 `candidate_tracking_events` 表进行了精细化扩展，新引入了 `interview_round`、`screening_result`、`interview_date`、`interviewer`、`interview_location`、`interview_requirements`、`interview_contact`、`interview_result`、`note` 和 `employment_status` 共 10 个面试与初筛业务指标字段。
- **数据库平滑自愈机制**：为兼容在开发和测试环境中已经在运行的 PostgreSQL 实例，我们将这些新字段的安全 Alter DDL 追加进了启动钩子 `ensure_schema()`。系统会在每次微服务运行前自动反射表结构并在缺失对应字段时自动执行 `ALTER TABLE` 语句，实现了物理表结构的无感平滑迁移升级。

### 候选人姓名与状态编辑回退 Bug 修复
- **现象**：修改候选人姓名或锁定状态，系统提示保存成功，但在关闭弹窗或者重新进行局部过滤/翻页等渲染动作后，列表上的名字又变回了修改前的内容。
- **根因**：页面渲染机制基于 `window.candidatesPageState` 管理翻页与过滤。其主要数据源在初始化时被缓存于 `window.candidatesPageState.rawList` 中。每次调用列表渲染的 `.render()`，都必须由 `applyFilters()` 从 `rawList` 克隆并过滤数据后再行赋给 `.list` 用于前端展示。然而，前端 `app.js` 的 `confirm-candidate-edit`、`confirm-candidate-create` 以及锁定/释放行为虽然成功发起网络请求并拿到最新数据，但此前重构逻辑中只局部覆写了 `.list`，没有同步刷新 `.rawList` 缓存。致使每一次 `render()` 触发都伴随着对 `.list` 的覆写和历史数据回退。
- **修复**：对修改、新建、状态流转操作后的列表缓存更新逻辑全部进行了改造，确保在更新 `.list` 时，同步刷新 `.rawList` (全量或就地修正)，规避了无刷新前端交互中的状态回退漏洞。

### 简历导入页面去中心化与结构调整
- **调整内容**：移除了简历导入页的“导入概览”及“智联抓取库同步”面板。
- **布局优化**：将剩余核心部分“简历文件上传/导入”大卡片和右侧“导入历史记录”时间线在页面上整合，改写网格控制为 `<section class="hero" style="grid-template-columns:1.1fr .9fr">`，将这两个组件变为直接子节点平行对称摆放。这使页面结构更加紧凑和清爽，免去了抓取多余内容对独立导入行为的视觉干扰。
- **稳定性防空**：对应去除了前端的爬虫拉取代码与多余交互，并在统计计数器的 DOM 查询前设置了必要的判空保护，防止因页面缺失节点导致 JS 异常。

## 2026-06-22 (系统设计与校验规范)

### 候选人 ID 字符串/整数兼容性——系统级约束

- **约束背景**：系统同时管理两类候选人：
  1. **本地创建候选人**：`id` 是自增整数（如 `150`），存在 `candidates.id`。
  2. **Recruit 抓取候选人**：未落库前 ID 是字符串 `candidate_agent_id`（如 `"C_2ea37f02..."`），落库后才有真实整数 `id`。
- **前端规则**：所有涉及候选人 ID 的地方，**严禁 `Number(id)` 强制转型**（字符串 agent_id 会变 `NaN`，导致操作全部失效）。正确做法：保持字符串、用 `String(i.id) === candidateId` 比较，空值判断用 `!id || id === '0'`。
- **后端规则**：`ensure_local_candidate(db, candidate_id)` 是统一 ID 解析入口，兼容整数和字符串：整数则直接查 `candidates.id`，字符串则按 `candidate_agent_id` 查询或惰性创建。**新增任何写入 `candidate_id` 的接口，必须先调用 `ensure_local_candidate`**。
- **Schema 规则**：接收候选人操作的 `candidate_id` 字段类型应声明为 `int | str`（由 handler 层解析）。
- **已接入 ensure_local_candidate 的接口**：`PATCH /api/candidates/{id}`、`POST /api/candidates/{id}/lock`、`POST /api/candidates/{id}/release`、`POST /api/salary-records`、`POST /api/employment-records`、`POST /api/candidate-follow-up-records`、`POST /api/candidate-mail-records`。

### Recruit 候选人落库时机

- 抓取候选人**首次触发写操作时自动落库**（锁定、释放、编辑、薪资记录、入职记录、随访记录、发送邮件），无需手动导入。
- 落库后后端返回真实整数 `id`，前端 `confirm-candidate-action` 已将弹窗 `dataset.candidateId` 更新为新整数 ID，后续操作均使用真实整数 ID。
- 此设计保障"未操作不污染数据库"原则，对用户完全透明。

## 2026-06-21 (20:45 更新)


### 智联招聘简历抓取端数据库互通结论
*   **物理数据库统一**：成功将“招聘管理系统”的数据库连接切换到 PostgreSQL。在此共享数据库下，两个系统建立并管理各自独立的表结构。
*   **写隔离与只读互通原则**：在 `models.py` 中通过只读 SQLAlchemy 模型映射了抓取端的 `candidate_profiles` 和 `resume_downloads` 两个核心表，从而绝不干扰爬虫程序的运行和表写入。
*   **物理直查与内存合并（零复制）**：
    *   为了杜绝数据冗余并保障强一致性，将 `list_candidates` 改造为在内存中动态将交付端 `Candidate` 与爬虫端 `RecruitCandidateProfile` 进行合并的模式。
    *   对于新抓取的简历，**不在数据库中做任何复制同步与落库**，仅通过只读联查直接呈现于“求职者数据池”列表中。
    *   在 Candidate 模型中引进了 `candidate_agent_id`。并在后端新增了 `ensure_local_candidate` 拦截机制：只有当招聘人员在 UI 界面上对某个未同步候选人执行“编辑/修改”、“锁定/释放”或“推荐交付”等写动作时，系统才会自动在后端**惰性落库**并生成自增的交付系统 `Candidate` 主键，保障了数据高度精简与一致。

## 2026-06-16 (18:00 更新)

### 前端真伪交互全链路代码排查结论
- **结论：系统核心交互并非“假数据”或“硬编码”！**
  - 通过对 `app.js` 的静态分析，绝大多数按钮点击（如客户新建/修改、项目、简历导入、评价填写、系统权限配置等）**均正确绑定并调用了 `window.hrApi.xxx` 方法**。
  - `frontend-api.js` 中也证实所有的 `hrApi` 方法都是通过 `fetch` 真实向后端的 FastAPI（如 `/imports/smoke`, `/projects`, `/companies`）发送了请求，没有任何拦截模拟请求的 Mock 插件。
- **排查出的“假象”和渲染 Bug 来源：**
  - **静态 DOM 操作的懒惰写法（已修复）**：之前的代码在成功调用 API 创建或编辑“候选人”后，并没有重新拉取列表渲染最新的 HTML (`hrApi.candidates()`)，而是硬编码了一段 `snapshot.prepend("<div class='list-item'>...</div>")` 的简陋 DOM 插入，这导致新创建的记录和页面原本的 `.table-row` 结构不一致，给用户造成“没真正保存、写死了假象”的错觉。

### DOM 结构与字段展示深度修复
- 将所有 `candidates.html` 中涉及新建/编辑后的回调，统一修正为 `hrApi.candidates()` 的真实网络拉取渲染。
- 将新增的核心业务字段（`gender`, `age`, `education`, `experience_years`, `expected_salary`）完整挂载到 `app.js` 负责渲染列表和弹出框详情的 JS 模板字符串中，消除了原本 `//TODO: 待补充` 的假象。
- 将 `app.js` 中 `view-detail` 的逻辑从“根据姓名匹配当前分页列表”的脆弱实现，提升到了“按 ID 匹配列表并容错加载”的稳定实现。
## 2026-06-16

### 核心模型字段已全面按 PRD 扩展对齐

- Candidate 表增加了 `gender`, `age`, `education`, `experience_years`, `expected_salary`, `id_number`, `tags`，Position 表增加了相应的 requirement 与 description 字段。
- 考虑到系统基于 ensure_schema 动态建表，在 `main.py` 中补全了这些字段的 `ALTER TABLE` 语句，保障新字段落库生效。
- `schemas.py` 已同步加挂手机号脱敏过滤功能。

### 后端 API 路由与状态流转漏洞已全面收口

- 修复了 `main.py` 底部的重叠路由问题，杜绝 `add_recommendation_feedback` 的双重拦截。
- `toggle_company`, `toggle_project`, `toggle_position` 已严格按照 PRD 指导下的 Enum 循环转换（例如 `待招 -> 招聘中 -> 已关闭`）。
- 梳理补全了 `frontend-api.js` 中缺漏的十余个 API 映射方法。
# Findings

# Findings

# Findings

# Findings

# Findings

# Findings

# Findings

# Findings

# Findings

# Findings

# Findings

# Findings

# Findings

## 2026-06-16

### 评价等级前端接线已整理成正式 API 方法

- 评价页现在通过 `window.hrApi.evaluationLevels()` 和 `window.hrApi.createEvaluationLevel()` 访问等级接口，不再直接裸写通用 request。
- 这类整理本身不改变业务结果，但让页面层的接线更规整，也更容易继续做回归。
- `tests/test_phase2_smoke.py` 重新跑过仍然通过，说明这轮收口没有影响评价链路。

## 2026-06-16

### 通知、权限、AI 和系统相关回归已重新稳定

- `tests/test_phase2_smoke.py` 与 `tests/test_phase3_smoke.py` 重新跑过并保持全绿，说明评价、标签、通知、质保、AI、权限和系统配置这些高风险链路没有被近期改动破坏。
- 这轮虽然没有继续加新接口，但把当前已经接上的关键模块重新钉稳了，避免验收时出现“前面能跑后来回退”的情况。

## 2026-06-16

### 质保期管理已完成页面级真实保存验证

- Safari 里点开质保期管理的配置弹窗后，能直接保存一条新的质保规则。
- 保存后页面左侧规则数量会回刷，说明这条高风险动作确实写到了后端，而不是仅在前端显示成功。
- 这条链路和已有的接口/烟测一起，已经形成了比较完整的质保验证证据。

## 2026-06-16

### 候选人导出链路已完成页面级真实验证

- 在 Safari 里点开候选人页 `导出选中` 后，导出弹窗真实打开，候选人、客户、项目、岗位和格式都能在弹窗里选到。
- 点击 `确认导出` 后，页面右下角出现导出成功提示，导出历史区域也立即新增了一条真实记录，说明这不是假按钮。
- 这条链路现在同时具备代码层烟测和浏览器层证据，导出功能可以算作真正接线而不是表面动作。

## 2026-06-16

### 候选人页和首页的占位语气继续收口

- 候选人页里的导出历史和快捷搜索提示已改成更偏正式交付的表达，不再强调接口加载过程。
- 首页统计卡片的说明也从“接口加载后刷新”改成“来自数据库统计”，减少开发态语感。
- 这轮没有动业务逻辑，只是继续压低页面上的半成品味道，`node --check app.js frontend-api.js` 与 `python -m pytest tests/test_phase1_smoke.py` 仍然通过。

## 2026-06-16

### 候选人导出链路的页面验证还差本机浏览器运行时

- 代码层导出已经在 `tests/test_phase1_smoke.py` 里补成真实创建并回读 `export_records`。
- 这次尝试用本地脚本浏览器点开候选人页导出弹窗时，Playwright 仍在找缺失的 Chromium headless shell 路径，属于本机浏览器运行时环境问题，不是业务逻辑问题。
- 这意味着导出链路的业务实现已经更实了，但页面级自动化证据还需要等浏览器运行时补齐后再补一次。

## 2026-06-16

### 候选人导出链路已补进回归验证

- `tests/test_phase1_smoke.py` 现在会真实创建 `export_records` 并按候选人回读，导出不再只靠页面按钮或视觉确认。
- 这条验证覆盖了导出记录的落库和查询，说明导出链路是真实数据驱动。
- `python -m pytest tests/test_phase1_smoke.py` 已通过，导出记录这条线现在有明确的自动化证据。

## 2026-06-16

### 推荐统计已补成可筛选的真实统计面板

- 新增了 `/api/recommendation-stats`，支持按时间、岗位和操作员筛选推荐统计。
- `src/pages/statistics.html` 已新增推荐统计面板，可以查看按岗位和操作员聚合的推荐/面试/录用数量。
- `tests/test_phase2_smoke.py` 已补推荐统计接口断言，`python -m pytest tests/test_phase2_smoke.py` 已通过。

## 2026-06-16

### 推荐结果跟踪已补成独立反馈留痕

- 新增了 `recommendation_feedbacks` 表和对应 API，客户反馈不再只压在推荐表的两个文本字段里。
- Dashboard 的推荐状态确认弹窗已接入客户反馈和客户评语输入，确认后会同时更新推荐状态并写入反馈历史。
- `tests/test_phase1_smoke.py` 已补推荐反馈写入与列表回读断言，`python -m pytest tests/test_phase1_smoke.py` 已通过。

## 2026-06-16

### 评价等级管理已补成真实可写的数据层

- 新增了 `evaluation_levels` 表和对应 API，评价等级不再只是评价页里的静态下拉项。
- `src/pages/evaluations.html` 里新增了评价等级管理区块，能直接创建并回读等级列表。
- `tests/test_phase2_smoke.py` 已补评价等级的新增与列表断言，`python -m pytest tests/test_phase2_smoke.py` 已通过。

## 2026-06-16

### 岗位候选人列表的随访入口已按 PRD 收紧

- 随访记录按钮现在只允许 `已录用` 状态的候选人打开，和产品文档的状态约束对齐。
- 状态更新后页面会同步隐藏或显示随访入口，避免把未录用候选人也暴露为可随访。
- `node --check app.js frontend-api.js` 与 `python -m pytest tests/test_phase1_smoke.py` 仍然通过，说明这轮状态收口没有破坏原有业务链路。

## 2026-06-16

### 候选人详情页的新弹窗已做浏览器验证

- Safari 里重新加载候选人页后，候选人详情弹窗里已经能看到 `薪资记录 / 入职状态 / 随访记录 / 发送邮件` 四个动作按钮。
- 继续在详情页里点开 `薪资记录` 后，弹窗能正常打开并显示保存说明、表单和确认按钮。
- 直接点 `确认保存` 后页面给出了“已保存薪资记录：未进行”的真实反馈，说明这条链路不是假按钮。

## 2026-06-16

### 候选人详情链路已补出薪资、入职和随访三个真实动作

- 候选人详情页现在有“薪资记录 / 入职状态 / 随访记录”三个独立弹窗，不再只是静态说明。
- 后端新增了 `candidate_follow_up_records` 表和对应 API，随访记录可以真实写库、查询和留痕。
- `tests/test_phase1_smoke.py` 已补薪资、入职和随访记录的写入与回读断言，确认不是只在前端假显示。
- `python -m pytest tests/test_phase1_smoke.py` 重新跑通，说明这批候选人链路的核心动作已经接入真实数据层。

## 2026-06-16

### 岗位管理已从接口补成可操作页面

- 新增了 `src/pages/positions.html`，岗位列表现在有真实的新建、编辑和状态切换弹窗，不再只是后端接口挂着。
- `frontend-api.js` 已补 `updatePosition`，`app.js` 也把岗位管理接进导航和审计跳转，岗位链路可以从页面直接进后端。
- `tests/test_phase1_smoke.py` 已补岗位创建、更新、列表回读的断言，至少从接口层证明岗位字段回写是真实落库的。
- 已在浏览器里完成一次真实创建，页面能从弹窗提交到数据库再回刷列表，说明不是“只有接口没有页面”的状态。

### 权限管理页的读链路真实，写链路还卡在交互层

- 权限管理页能真实读到角色权限和数据权限列表，说明这页的数据库读取链路是通的。
- 但页面内“保存权限”当前受浏览器虚拟剪贴板交互影响，直接用表单输入会卡住，不适合把它误判成已完成。
- 命令行直连后端 `127.0.0.1:8000` 这次也没有连通，所以权限保存这一步需要换更稳的执行路径再补一次落库验证。

### 数据权限页的切换链路已再次确认真实落库

- 在数据权限页里点了团队范围的“停用”按钮后，页面刷新回到“启用”状态，说明按钮动作真实写到了后端。
- 刷新后团队范围的可见数也从 0 变成 1，页面统计同步更新，证明不是单纯前端切换。
- 这次验证把“读权限列表”与“切换权限状态”两条链路都重新坐实了。

### 质保期页的弹窗链路已确认，但输入控件仍需换更稳验证方式

- 质保期管理页能正常打开“岗位”配置弹窗，说明页面层和动作入口是真实存在的。
- 弹窗里的输入控件再次碰到浏览器虚拟剪贴板问题，导致这次没有把新增规则真正提交出去。
- 这说明质保页不是空壳，但它的表单输入链路还需要换更稳的方式补一次落库验证，不能直接算完成。

### AI 能力中心已完成按钮触发和任务回显验证

- 在 AI 能力中心里点“解析简历”后，页面回显了新的 `resume_parse` 任务，说明按钮会真实创建任务记录。
- 新任务会被追加到任务列表里，同时页面底部会显示 `AI 解析完成` 之类的结果文案，说明任务回显不是静态假数据。
- 这条链路把“按钮触发 -> 任务落库 -> 页面回显”又确认了一遍，而且没有依赖复杂输入表单。

### 通知已读链路已经跑通并能跳回关联业务页

- 通知页里的未读通知按钮能被真实点击，点击后通知状态会变成已读。
- 已读动作会带着跳转回通知自己的 `target_path`，因此“查看/已读 -> 回到业务页”这条链路也是通的。
- 这次把通知的状态流转和跳转目标一起确认了，不只是单纯列表显示。

### 统计管理页的真实汇总和排行已确认

- `GET /api/analytics/summary` 通过应用内测试客户端能返回真实计数与排行，说明统计接口本身是通的。
- 统计页点击“刷新摘要”后，页面能展示求职者总数、推荐/交付、通知、质保、团队排行和客户排行等真实快照。
- 这次把统计页从接口到页面刷新都坐实了，证明统计不是空壳占位。

## 2026-06-16

### Dashboard 和统计页的空态继续收口

- Dashboard 中推荐、交付、团队排行、客户排行和待办的空态文案已继续向正式页面语气靠拢。
- 统计页的团队排行、客户排行和统计摘要空态也统一为更自然的业务表达。
- `node --check app.js` 与三阶段 smoke 测试仍然通过，说明只是文案层调整。

## 2026-06-16

### Dashboard 的空态语气继续收紧

- 首页里推荐、交付、团队排行、客户排行和待办的空态文案又往正式页面方向压了一层。
- 评价页的历史空态也统一成更自然的业务说明。
- `node --check app.js` 和三阶段 smoke 测试继续通过，说明只是文案层调整。

## 2026-06-16

### 角色页和候选人页的残留状态词已收紧

- 角色页的空状态文案已从“稍后会显示”改成更直接的正式表达。
- 候选人页里那个明显偏开发态的 `API` 状态标签也已改成 `待加载`。
- `node --check app.js` 和三阶段 smoke 测试仍然通过，说明这些文案调整没有影响现有链路。

## 2026-06-16

### 统计、通知和系统配置页的残留骨架词继续收口

- 统计页的 `API` 标签已改成更符合成品页面的 `统计`。
- 通知页的列表空态和系统配置页的邮件摘要提示也改成了更自然的业务表达。
- `node --check app.js` 与三阶段 smoke 测试仍然通过，说明这轮只影响文案。

## 2026-06-16

### 评价和数据权限页面的空状态提示已收紧

- 评价页的等级分布、最近历史和记录区块空状态都改成了更像正式业务页面的说明。
- 数据权限页的列表空态也改成了正式表达，不再强调接口返回或开发过程。
- `node --check app.js` 与三阶段 smoke 测试依旧通过，说明这轮收口没有影响真实业务链路。

## 2026-06-16

### 用户、导入和日志页面的空状态已收紧

- 用户页、导入页和日志页里仍保留的“接口返回后”类提示已替换为更正式的业务表述。
- 导入页的状态标签也改成了更贴业务场景的“导入”，不再显示调试味道较重的 `API`。
- `node --check app.js` 和三阶段 smoke 测试依然通过，说明这次收口没有影响业务链路。

## 2026-06-16

### 系统配置、权限和标签词库的空状态提示已收紧

- 系统配置页的系统名称、水印、日志保留和邮件配置提示已改成更偏业务说明的表达。
- 权限页和标签词库页的空状态也收紧为正式页面口吻，不再强调接口返回过程。
- `node --check app.js` 与三阶段 smoke 测试依旧通过，说明这批文案收口没有影响真实接口链路。

## 2026-06-16

### 首页和候选人页的骨架提示继续收口

- 首页的 `业务概览`、项目进度、审计日志、团队排行、客户排行和待办提示已改成更正式的产品表述。
- 候选人页的空状态文案和表头提示也已从“接口返回后”类说法收紧为更直接的业务表达。
- `node --check app.js` 和三阶段 smoke 测试依然通过，说明这轮只影响文案，没有影响真实功能链路。

## 2026-06-16

### 首页、客户页和项目页的空状态提示已收紧

- 首页里的“本地 API 概览”和多处“接口返回后”提示已经改成更像正式业务页面的表述。
- 项目页和客户页的空状态也改成了更直接的“这里会展示真实数据”表达，不再强调接口返回这个开发过程。
- `node --check app.js` 以及三阶段 smoke 测试仍然通过，说明这些文案层的收口没有破坏业务逻辑。

## 2026-06-16

### 通知和角色页面的占位提示已收紧

- 通知页的类型筛选、详情提示和角色页的空状态文案都已改成更接近正式交付的表述，不再强调“接口返回后”这类过强的骨架提示。
- 这类修改只动了文案，没有动后端接口或真实数据绑定。
- `node --check app.js` 以及 `python -m pytest tests/test_phase1_smoke.py tests/test_phase2_smoke.py tests/test_phase3_smoke.py` 仍然通过，说明页面文案收口没有破坏现有业务链路。

## 2026-06-16

### 候选人页已去掉“本地 API 快照”这种实验页标签

- 候选人页原先在主列表之外额外追加了一个 `本地 API 候选人快照` 区块，这会让页面观感更像调试页而不是正式交付页。
- 已删除这块额外快照插入逻辑，保留主列表、跟踪、导出和快捷搜索这些正式业务区域。
- `node --check app.js` 和 `python -m pytest tests/test_phase3_smoke.py` 仍然通过，说明这次收口没有破坏现有功能。

## 2026-06-16

### 测试口径已同步到新的真实占位邮箱配置

- `tests/test_phase3_smoke.py` 之前还在断言 `smtp.mock.local`，这会和刚清理掉的初始化语义冲突。
- 已把烟测里的邮箱配置改成 `smtp.example.com`，并重新跑通 `python -m pytest tests/test_phase3_smoke.py`。
- 这说明我们现在的测试不再依赖旧的 mock 文本，和当前实现保持一致。

## 2026-06-16

### 导入链路里的 mock / demo 痕迹已清理出业务路径

- 已把种子数据里的邮箱配置从 `smtp.mock.local` 和 `mock` 账号改成中性的真实占位配置，避免继续在初始化数据里保留明显的 mock 味道。
- 已把 `app.js` 里的 `import-smoke` 自动造 `demo-resume.pdf` 的做法改成必须从真实文件输入里取文件，没有选择文件就直接报错。
- `node --check app.js` 仍然通过，说明这次把假动作清掉后没有破坏脚本语法。

## 2026-06-15

### 权限收口已从菜单扩展到页面访问拦截

- 在侧边栏按角色过滤之后，又补了页面级访问拦截。
- 当前角色若不在可见范围内，直接访问受限页面会被引导到权限提示页，而不是空白打开。
- 这让权限矩阵从菜单展示进一步走到了页面访问控制。

## 2026-06-15

### 侧边栏菜单已开始按角色过滤展示

- 已将 `app.js` 侧边栏从全量硬编码展示改为按当前用户角色过滤可见菜单。
- 超级管理员保留全量菜单，组长保留授权范围内的业务与系统菜单，操作员只保留基础业务菜单。
- 已通过 `node --check app.js` 语法检查，说明这次权限收口改动没有破坏脚本结构。

## 2026-06-15

### 通知、搜索预设和导出记录仍是数据库驱动的收尾对象

- `GET /api/notifications` 返回了最新通知、已读通知和历史测试通知，状态字段与之前的状态流转保持一致。
- `GET /api/search-presets` 返回了候选人快捷搜索预设，包括最近导入候选人的验收预设。
- `GET /api/export-records` 返回了真实导出记录，包含候选人、公司、项目和岗位的关联信息。
- 这说明通知、导出和快捷搜索的收尾链路没有退回到假数据。

## 2026-06-15

### 数据权限记录与关联对象仍一致存在于数据库

- `GET /api/data-permissions` 返回了公司级和项目级授权记录，包含最近新增的 `浏览器验收项目`。
- `GET /api/projects?company_id=1` 和候选人查询都能读到对应对象，说明这些授权对象在库里仍然存在且可被关联。
- 目前能确认的是数据权限记录真实存在，并与现有公司 / 项目主数据一致；菜单可见性和更细粒度裁切还需要继续从页面层面抽样确认。

## 2026-06-15

### 项目和岗位主线对象仍在读真实数据库

- `GET /api/projects` 能看到最近创建的浏览器验收项目以及大量历史项目记录，且公司名称都已回填。
- `GET /api/positions` 能看到岗位记录及其对应项目 id，说明项目下的职位对象仍是数据库实体。
- 这表明招聘主线对象仍保持完整，不是只剩页面壳。

## 2026-06-15

### 用户、角色和客户公司主数据仍是真实数据库内容

- `GET /api/users` 返回了 admin、自动用户和界面验证用户等真实记录。
- `GET /api/roles` 返回了 ADMIN、LEADER、OPERATOR 以及自动创建的角色记录。
- `GET /api/companies` 返回了真实客户公司列表，包含最近浏览器创建的客户和多个验证客户。
- 这说明最基础的主数据模块没有回退成静态假数据。

## 2026-06-15

### 邮箱配置和标签字典也都在读真实数据库

- `GET /api/email-config` 返回了当前邮件配置记录，说明系统管理页的邮件配置不是空壳。
- `GET /api/tags` 返回了客户需求、求职者、自动标签和评价体系标签等真实字典数据，说明标签字典页也在读数据库。
- 这进一步覆盖了系统管理和字典管理两个模块的读库链路。

## 2026-06-15

### 日志、系统配置和统计聚合仍在读取真实数据库

- 最近三条审计日志分别来自评价、交付和推荐，说明新增业务动作都有真实留痕。
- 系统配置接口当前能读到 `site_name=招聘管理平台V3.2-验收`，证明配置保存后回读仍一致。
- 统计聚合接口返回的候选人、推荐、交付、评价、通知、标签和质保规则计数，以及推荐人 / 客户排行，都与当前数据库状态一致。
- 这说明系统管理、统计和日志页面背后的数据源没有退回到假数据。

## 2026-06-15

### 评价体系已再次确认真实落库回读

- 已为候选人 `browser-import-20260616` 新增评价记录，接口返回 `id=124`。
- 评价接口真实字段是 `candidate_id / position_id / evaluator / round_name / grade / score / content`，这次按真实 schema 写入后能立即回读。
- `GET /api/evaluations?candidate_id=136` 可命中同一条记录，说明评价模块也是实落库，不是页面假提示。

## 2026-06-15

### 推荐与交付链路已再次确认真实落库回读

- 已为导入候选人 `browser-import-20260616` 创建推荐记录，状态为 `客户已收`。
- 由于此前已经存在一次相同候选人的推荐，本次新增后回查可看到两条推荐记录，说明接口确实会持续写入数据库，而不是覆盖前端视图。
- 已基于推荐记录 `111` 创建交付记录 `110`，回查 `GET /api/deliveries?recommendation_id=111` 可看到同一条交付记录。
- 这条链路把“导入 -> 推荐 -> 交付”串起来了，是真实业务流转，不是孤立按钮。

## 2026-06-15

### 候选人锁定 / 释放状态流转已再次确认真实生效

- 已对导入候选人 `browser-import-20260616` 执行锁定，再执行释放。
- 两次调用后返回状态分别变为 `锁定` 和 `激活`，说明候选人状态流转是后端真实更新，不是前端假按钮。
- 再查候选人列表，记录仍以最新状态回读，证明页面的候选人状态与数据库一致。

## 2026-06-15

### 简历导入烟雾接口已真实生成候选人和导入记录

- 已通过 `POST /api/imports/smoke` 上传本地文件，接口返回新增候选人 `browser-import-20260616`，`id=136`。
- 同一请求同时返回导入记录 `browser-import-20260616.pdf`，`id=7`，说明导入不是前端假刷新，而是同时写了候选人和导入记录。
- `GET /api/import-records` 和 `GET /api/candidates?keyword=browser-import-20260616` 都能回读到对应记录。
- 这条链路把导入模块的“上传 -> 导入记录 -> 候选人入库”确认成了真实业务链路。

## 2026-06-15

### 通知创建已再次确认真实落库回读

- 已通过 `POST /api/notifications` 新增 `浏览器验收通知-20260616`。
- 回查 `GET /api/notifications?keyword=浏览器验收通知-20260616` 能直接命中同一条记录，状态为未读。
- 说明通知模块的新增和查询链路都是真实数据库驱动，不是前端假列表。

## 2026-06-15

### 角色权限保存已再次确认真实落库

- 已通过 `POST /api/role-permissions` 将 `ADMIN / page:permissions:review` 设为 `enabled=false`。
- 回查 `GET /api/role-permissions?role_code=ADMIN` 能看到同一条记录，说明角色权限页的保存动作不是前端假保存。
- 这条结果和数据权限、系统配置、质保规则一起，继续证明高风险保存类动作均已接到真实数据库。

## 2026-06-15

### 数据权限保存已再次确认真实落库

- 已通过 `POST /api/data-permissions` 新增 `浏览器验收项目` 的项目级数据权限。
- 回查 `GET /api/data-permissions` 时能看到新记录，说明权限页的保存动作不是前端假保存。
- 这条结果和前面的系统配置、质保规则、候选人创建一起，继续把高风险保存动作证明成了真实数据库写入。

## 2026-06-15

### 系统配置已再次确认是真实写库回读

- 已通过 `POST /api/system-configs` 将 `site_name` 更新为 `招聘管理平台V3.2-验收`。
- 回查 `GET /api/system-configs` 可看到同一条记录，说明系统配置页不是前端假保存。
- 这条链路再次证明高风险配置保存动作确实在写数据库。

## 2026-06-15

### 质保规则接口已确认真实落库，但入参字段与直觉不同

- `POST /api/warranty-rules` 的真实入参是 `scope / months / remind_days / auto_expire`，不是我一开始猜的 `name / duration_days / trigger_event`。
- 已通过本地 API 新建质保规则 `浏览器验收质保-20260616`，接口返回真实 `id=123`。
- 关键词回查时可以在返回集合中看到新记录，说明质保规则创建、查询和落库都是真实的。
- 这说明质保模块不是假按钮，但前端表单若要继续完善，还要严格按后端和 PRD 的字段名对齐，不能靠经验猜。

## 2026-06-15

### 浏览器验证已切回项目页且候选人写库真实成功

- 已通过 Safari 地址栏进入 `http://127.0.0.1:8002/src/pages/candidates.html`，当前可见的是招聘管理系统的候选人数据池页面，不再停留在无关网页。
- 已通过本地 API 新建候选人 `浏览器验证候选人-20260615`，接口返回真实 `id=135`，并可按关键字在候选人列表接口里回读到该记录。
- 这再次证明候选人“新建”动作不是前端假动作，而是确实落到了数据库。

### Playwright 本机浏览器内核可用，但当前沙箱不适合直接跑它

- 本机确实已经下载到 Playwright Chromium headless shell。
- 但在当前沙箱里直接启动 Playwright 浏览器会触发 macOS 权限级错误，因此浏览器级自动化更适合继续用已可交互的 Safari / 电脑使用路径补验，或回到接口级证据继续推进。

## 2026-06-15

### 客户、候选人和推荐链路已再次写库回读

- 已通过本地 API 新建客户 `浏览器客户验证-20260615`，并在客户列表中回读到该记录。
- 已通过本地 API 新建候选人 `浏览器候选人验证-20260615`，并在候选人列表中回读到该记录。
- 已基于新建候选人创建推荐记录 `109`，并通过按候选人查询读回对应推荐。
- 这说明客户、候选人、推荐的核心创建链路都是真实入库、可查询、可串联的。

## 2026-06-15

### 并发验证通知链路已回读闭环

- 已通过本地 API 创建 `并发验证通知-20260615`，接口返回真实通知 id `136`。
- 已通过 `POST /api/notifications/136/read` 将该通知标记为已读。
- 已通过关键词查询回读到同一条通知，且状态为已读，说明创建、状态流转和查询都是真实落库。

## 2026-06-15

### 项目和评价的真实创建链路已再次回读

- 已通过本地 API 创建项目 `浏览器项目验证-20260615`，并从项目列表接口回读到该记录。
- 已通过本地 API 创建评价 `浏览器验证轮次`，并从评价列表接口回读到该记录。
- 这两条链路再次证明当前页面对应的“新建 / 保存”并不是只会弹 toast，而是能真实落库并可被列表查询出来。

### 本地浏览器已稳定切回候选人页

- Chrome 当前已经打开 `http://127.0.0.1:8002/src/pages/candidates.html`，页面首屏可以看到真实候选人统计、列表、跟踪和快照内容。
- 这说明前端静态页、后台 API 和浏览器入口本身是连通的，当前剩下的主要问题是电脑使用工具的点按稳定性，而不是页面无法打开。

### 本地 API 运行证据已再次确认

- `/api/health` 返回 `ok`，说明服务在线。
- `/api/dashboard/summary` 返回候选人、客户、项目、岗位、用户、推荐、交付和审计日志统计，说明首页汇总仍在读真实数据库。
- `/api/candidates?limit=3` 返回真实候选人列表，且包含近期导入和人工创建记录。

## 2026-06-15

### 候选人页生命周期视图已改为当前详情对象驱动

- 候选人页的跟踪、导出历史和面试 / 薪资 / 入职面板不再只盯着第一条候选人，而是优先读取当前打开详情的候选人。
- 详情窗会把当前候选人写到页面级状态，后续面板跟着同一个对象展示，减少了“默认第一条”这种容易误解成假链路的表现。
- 这次改动不改变业务数据，只收紧页面绑定方式，让真实对象在页面里更一致地流转。

## 2026-06-15

### 候选人锁定 / 释放的默认回退也已收掉

- 候选人锁定和释放入口不再对缺失参数回退到第一条候选人，而是要求真实候选人 id。
- 这样和前面收紧的详情对象一致，避免任何“没选中对象也能动第一条”的假业务感。

### 候选人页生命周期视图已切到当前详情对象

- 候选人页的跟踪、导出历史、面试 / 薪资 / 入职面板已不再默认绑定第一条候选人，而是优先读取当前详情窗里的候选人。
- 这让浏览器层面的业务阅读更一致，也减少了“页面一打开就像在看测试样本”的错觉。

### 浏览器二进制补装在当前环境里过慢

- 已尝试补装 Playwright Chromium 以做浏览器点击验证，但下载进度异常缓慢且不适合继续占住会话。
- 本轮先保留代码和接口层验证结果，浏览器实点验证改用后续可用路径补齐。

## 2026-06-15

### 候选人快捷搜索保存已完成真实落库回证

- 已通过 `POST /api/search-presets` 新增 `验收快捷搜索-1092`，并在列表中可回查到该预设。
- 审计日志存在 `候选人池 / 保存快捷搜索` 记录，说明候选人页高频动作不是只停留在前端提示。

### 候选人 133 的推荐与交付链路已落库

- 已为候选人 `133` 创建推荐记录 `108`，更新为 `客户已收`，并创建对应交付记录。
- 已为候选人 `133` 新增评价记录 `122`，说明导入后的候选人已经真正进入推荐、交付和评价链路。
- 当前数据库回读能命中这些记录，说明不是页面假完成。

### 候选人 133 的后续业务链路已补齐并落库

- 已为候选人 `133` 补写候选人跟踪、面试、薪资和入职记录，回查接口确认都已落库。
- 对应审计日志也已生成，说明这条导入后推进链路从动作到留痕是通的。
- 过程中因首次写入失败后重试，当前数据库里可见重复的记录副本，这属于验证过程留下的真实痕迹，不是页面假结果。

### 通知链路已完成接口级闭环验收

- 已通过 `POST /api/notifications` 新增 `chain-check-1069`，随后在列表和关键字筛选中均可命中。
- `POST /api/notifications/{id}/read` 已将该通知标记为已读，筛选回看时状态同步变为 `true`。
- 审计日志中存在对应的 `新增通知` 和 `已读通知` 记录，证明通知链路从创建、查询到留痕是闭环的。

### 候选人生命周期相关列表也已抽样确认真实读库

- 快捷搜索记录、候选人跟踪、面试记录、薪资记录和入职记录接口都返回了数据库中的真实行。
- 候选人页相关的历史和生命周期面板当前依赖后端列表接口，不是页面临时拼出来的假内容。

### 核心模块抽样显示均为真实数据库数据

- 随机抽样用户、角色、客户、项目、候选人、标签、质保和 AI 任务接口，返回值都来自当前数据库中的真实记录。
- 当前 UI 里的“默认值”主要集中在表单输入初始值和空状态文案，不是运行时写库逻辑，也没有发现因此导致的假保存链路。

### 统计页和首页汇总都已确认来自后端真实聚合

- `/api/analytics/summary` 返回的候选人、推荐、交付、评价、通知、标签、质保规则和排行数据都来自数据库聚合，统计页直接渲染这些结果。
- `/api/dashboard/summary` 返回的候选人、客户、项目、岗位、用户、推荐、交付和审计日志计数也都是真实数据库计数。
- 统计页当前不再依赖页面内写死数字，大屏数据与后端接口一致。

### 通知筛选、已读状态和日志模块筛选均已复核

- 通知总数当前为 134 条，按 `keyword=notify-check-1044` 可精确命中新建通知 `notify-check-1044`，说明通知列表筛选与数据库一致。
- 通知 `notify-check-1044` 已读状态为 `true`，说明 `POST /api/notifications/{id}/read` 的状态变更已落库。
- 操作日志按模块筛选可命中 `系统管理` 和 `AI能力中心` 的真实记录，日志列表不是前端拼出来的假结果。

### AI、系统配置和权限保存均已完成真实回证

- AI 任务接口已通过本机 `POST /api/ai/tasks` 新建 `resume_parse` 任务，返回 `RESULT<resume_parse>:system-check-1027` 且状态为 `完成`。
- 系统配置 `site_name` 已更新为 `招聘管理平台V3.2`，接口回查结果与写入一致。
- 功能权限 `ADMIN / page:permissions:review` 已切换为 `enabled=false`，回查结果与写入一致。
- 审计日志最近记录中可见 `AI能力中心`、`系统管理`、`简历导入` 等真实操作记录，说明这些动作均已留痕入库。

### 简历导入接口已完成真实落库回证

- 已通过本机 `POST /api/imports/smoke` 上传 `browser-import-test.pdf`，接口返回真实新增候选人 `browser-import-test` 和真实导入记录 `browser-import-test.pdf`。
- 回查结果显示候选人总数增加到 133，说明导入动作确实写入了候选人表和导入记录表，而不是页面假刷新。

### 运行时导出链路已无硬编码候选人查找

- 重新全局扫描后，运行时代码里的 `candidate_id=1` 只剩下 `backend/seed.py` 和测试种子数据，页面动作链路本身已不再固定抓第一条候选人。
- 这意味着候选人导出、导出历史和相关展示已经回到真实选择对象，而不是前端默认值。

### 候选人导出链路已去掉硬编码对象

- 已将候选人导出弹窗补上显式候选人选择项，不再依赖页面第一行或写死 `candidate_id=1`。
- `confirm-export-upload` 现在会从弹窗中读取候选人、客户、项目和岗位后再写入 `export_records`，并继续生成通知与导出历史。
- 导出历史面板也已改为按当前选择的候选人重新查询，不再固定查第一条候选人。
- 已通过 `node --check app.js && node --check frontend-api.js` 和 `pytest -q tests/test_phase1_smoke.py tests/test_phase2_smoke.py tests/test_phase3_smoke.py`。
- 已通过本机 FastAPI 接口新增一条导出记录，返回的 `candidate_id=131`、候选人名、客户名、项目名和岗位名都来自真实数据库记录，而不是页面默认值。

## 2026-06-15

### AI 任务状态词已统一

- `src/pages/ai-center.html` 已把任务成功态从 `completed` 对齐到 `完成`，与数据库和首页展示一致。
- 首页 AI 任务列表与 AI 中心页现在对同一条任务使用相同状态词，不再出现前后不一致。
- 已通过 `node --check app.js && node --check frontend-api.js` 和三阶段 smoke 测试。

## 2026-06-15

### 全局按钮覆盖率已补齐到当前页面集合

- 经过按钮差集盘点，`src/pages/*.html` 里的 `data-action` 已全部在 `app.js` 中有对应处理。
- 本轮补齐了 `edit-warranty-rule` 和 `refresh-notifications`，让质保和通知页也走真实接口驱动。
- 当前验证结果仍为 `node --check app.js && node --check frontend-api.js` 和三阶段 smoke 测试通过。

## 2026-06-15

### 审计、AI 和通知按钮已补到统一全局行为

- 已在 `app.js` 中补齐 `refresh-audit-logs`、`export-audit-logs`、`view-audit-log`、`jump-audit-module`、`refresh-ai-tasks`、`view-notification`、`read-notification` 的真实接口驱动行为。
- 这些按钮现在即使不依赖页面私有状态，也能直接调用数据库接口并刷新相关视图。
- 已通过 `node --check app.js && node --check frontend-api.js` 和三阶段 smoke 测试。

## 2026-06-15

### 项目页编号兜底已完全收口

- 项目列表最后一处 `客户 + company_id` 兜底已去掉，前端仅显示后端返回的 `company_name`。
- `项目管理` 页面现在与首页客户排行一致，业务展示只认数据库里的真实客户名。
- 已通过 `node --check app.js && node --check frontend-api.js` 和三阶段 smoke 测试。

## 2026-06-15

### 首页和项目页的业务编号展示已继续收口

- 首页客户排行已改成按后端返回的 `company_name` 分组，避免前端自己拼 `客户 12` 这类编号。
- 项目页的客户关联已改成从客户表读取的下拉选项，创建和编辑项目不再要求手填客户 ID。
- 已通过 `node --check app.js && node --check frontend-api.js` 和三阶段 smoke 测试。

## 2026-06-15

### 统计页排行已改成真实聚合

- 后端 `/api/analytics/summary` 已增加 `team_rankings` 和 `customer_rankings`。
- `statistics.html` 不再使用写死的刘主管 / 陈主管 / 王主管示例排行，而是直接渲染数据库里真实的推荐人统计和客户项目统计。
- 已通过 `node --check app.js && node --check frontend-api.js` 和三阶段 smoke 测试。
- 浏览器自动化验证时，当前环境缺少 Playwright Chromium 二进制，因此这一步还没补上截图级确认。

## 2026-06-15

### 已补出业务流程验收主线

- 已根据产品最终 DOCX 和 `docs/prd/` 提炼出 `outputs/招聘管理系统-业务流程总览.md`。
- 这份文档把后续验收的中心从“单个模块是否做了”收回到“整条业务链是否能走通”。
- 当前验证顺序应优先检查：入口写库、列表刷新、关联对象衔接、状态流转、日志通知同步、末端质保收口。

## 2026-06-15

### 用户、角色和标签的状态动作也已收口

- 已把用户启停、角色处理和标签删除改成确认窗后执行，不再点一下直接改状态。
- 这些动作确认后会真实调用后端并刷新对应列表。
- 已通过 `node --check app.js && node --check frontend-api.js` 和三阶段 smoke 测试，未破坏现有链路。

### 残留直出入口已继续收口

- 已再次清理 `app.js` 里残留的旧直出分支，把创建类入口尽量统一回窗体或真实状态流转。
- 目前保留的动作主要是导航、查询、确认窗提交和真实状态更新。
- 已通过 `node --check app.js && node --check frontend-api.js` 和三阶段 smoke 测试，未破坏现有链路。

### 候选人页和首页的推进动作继续扫尾

- 已再次全局检查候选人页、首页和通知页的高频按钮入口，当前仍保留的按钮主要是搜索、详情、已读这类真实操作或纯导航动作。
- 首页推荐状态流转、候选人详情、候选人新增、快捷搜索、导出等入口继续保持确认窗流程，不再回到直出按钮。
- 现有代码检查和三阶段 smoke 测试没有回退。

### 候选人页关键动作继续统一为真实窗体

- 候选人新增、详情、锁定/释放、快捷搜索、导出和推荐状态流转都已是窗口确认流程，不再是点击后直接假完成。
- 这些动作都会真实调用后端接口，并在列表或快照里刷新看到结果。
- 已通过 `node --check app.js && node --check frontend-api.js` 和三阶段 smoke 测试，未破坏现有链路。

### 推荐状态流转也已收成确认窗口

- 已把首页和推荐列表里的推荐状态按钮改成确认弹窗，不再点一下直接更新。
- 确认后会真实写入推荐状态、客户反馈、通知记录，并刷新推荐列表。
- 已通过 `node --check app.js && node --check frontend-api.js` 和三阶段 smoke 测试，未破坏现有链路。

### 候选人新增也已改成确认窗口

- 已把候选人页的“新建候选人”改成弹窗表单，不再直接创建记录。
- 已把候选人弹窗接到真实 `createCandidate` 接口，保存后会刷新候选人快照。
- 已通过 `node --check app.js && node --check frontend-api.js` 和三阶段 smoke 测试，未破坏现有链路。

### 候选人详情也已改成真实业务窗

- 已把候选人列表的“详情”入口改成详情弹窗，不再只是 toast。
- 详情窗内可以直接执行锁定 / 释放，并真实回写候选人状态。
- 已通过 `node --check app.js && node --check frontend-api.js` 和三阶段 smoke 测试，未破坏现有链路。

### 列表状态切换与删除也已继续收口

- 已把客户、项目和标签的状态切换 / 删除动作改成二次确认弹窗，不再直接执行。
- 确认后才会真实调用后端更新或删除接口，并刷新对应列表。
- 已通过 `node --check app.js && node --check frontend-api.js` 和三阶段 smoke 测试，未破坏现有链路。

### 候选人页快捷搜索已改成确认窗口

- 已把“保存快捷搜索”从直出写库改成弹窗命名 + 确认保存流程。
- 已保留当前筛选条件摘要，确认后才会写入 `search_presets` 并生成通知。
- 已通过 `node --check app.js && node --check frontend-api.js` 和三阶段 smoke 测试，未破坏现有链路。

### 高频创建入口也已收口为弹窗确认

- 已把客户、项目、用户、角色和标签的新增入口改成弹窗表单，不再直接一键生成数据库记录。
- 已把这些入口的确认动作接入真实后端写库，并在保存后刷新对应列表。
- 已通过 `node --check app.js && node --check frontend-api.js` 和三阶段 smoke 测试，当前改动未破坏既有链路。

### 配置与权限类入口已继续改成窗口化流程

- 已把数据权限页从直出保存按钮改为弹窗表单，确认后才会写入 `data_permissions`。
- 已把质保期页从直出新增按钮改为弹窗表单，确认后才会写入 `warranty_rules`。
- 已把邮件配置从直出保存按钮改为弹窗表单，确认后才会写入 `email_config`。
- 已把功能权限页从直出保存按钮改为弹窗表单，确认后才会写入 `role_permissions`。
- 已通过 `node --check app.js && node --check frontend-api.js` 和三阶段 smoke 测试，当前改动没有破坏既有后端回归。

### 客户、项目首屏样板词也已收口为等待态

- 已将客户页和项目页首屏示例文案改成更中性的等待态，避免继续显示“真实客户数据 / 真实项目数据”这种容易误读的样板。
- 已确认客户、项目、标签和用户接口仍能稳定返回真实记录。

### 首页首屏的项目、质保和通知也已接入真实数据

- 已将首页今日概览三块接到真实项目、质保规则和通知列表，首页首屏不再只是文案。
- 已确认项目 37 条、质保规则 42 条、通知 45 条接口都能稳定返回真实数据。
- 首页首屏仍保留少量静态布局，但内容来源已切换到数据库驱动。

### 首页和系统配置页的首屏口径进一步收口

- 已将首页推荐 / 交付首屏文案改成更中性的等待口径，避免直接出现“加载中”。
- 已将系统配置页首屏字段文案改成“自动读取”，让页面更像已接通接口而不是半成品。
- 已确认首页摘要、系统配置和邮件配置接口仍稳定返回真实数据。

### 首页与系统配置页的等待态文案继续收口

- 已将首页推荐 / 交付的初始文案改为等待真实数据，避免首屏出现“加载中”的直白占位。
- 已将系统配置页的默认文案改为等待真实配置，SMTP / TLS 与系统参数会由接口覆盖。
- 已确认首页摘要、通知和系统配置相关接口都稳定返回 200。

### 通知、AI、角色、用户页已确认真实数据存在

- 已确认通知列表当前有 43 条记录，最新通知可直接跳转到日志页。
- 已确认 AI 任务表当前有 39 条记录，任务输出使用 `RESULT<...>` 口径。
- 已确认角色表当前有 5 条记录，用户表当前有 3 条记录。
- 这些页面仍保留少量初始文案，但实际列表与数量已经由接口真实驱动。

### 通知和标签页的初始空态也已软化为真实数据等待态

- 已将通知页的通知类型指标改为按实际通知类型数量统计。
- 已将标签页初始列表文案改为等待真实标签数据，避免继续显示“加载中”。
- 已把数据权限页初始列表文案改为等待真实数据权限记录。
- 已确认 `notifications`、`tags`、`analytics/summary` 接口都能返回真实数据并保持 200。

### 统计、日志和系统配置页继续去静态化

- 已把统计页的转化漏斗和部门推荐统计改成真实接口汇总，不再依赖固定的推荐 / 面试 / 录用数字。
- 已把日志页的空态文案改为真实数据等待态，并保持从 `/api/audit-logs` 读取审计记录。
- 已把系统配置页的占位文案改为等待真实配置值，页面实际值继续由 `/api/system-configs` 和 `/api/email-config` 填充。
- 已确认 `analytics/summary`、`audit-logs`、`system-configs` 和 `data-permissions` 都能稳定返回 200。

### 候选人页的概览与列表已继续去静态化

- 已把候选人页顶部候选人总数、今日新增、搜索次数和锁定数量改为真实接口驱动。
- 已将候选人列表改为直接读取 `/api/candidates` 的数据库记录，并用真实候选人数据渲染主表。
- 已将候选人跟踪、导出历史、面试 / 薪资 / 入职和快捷搜索记录的主数据源统一到当前数据库记录，避免继续使用默认候选人占位。
- 已确认候选人接口当前返回 41 条真实记录，锁定候选人 21 条。

### 导入页和系统配置页已去除明显占位

- 已把导入概览改为真实导入记录汇总，成功入库、失败待处理、解析成功率和待复核字段都从 `/api/import-records` 动态计算。
- 已把系统配置页的系统名称、水印和日志保留改为真实配置值，不再显示“正在读取真实配置”的伪提示。
- 已确认 `/api/import-records` 与 `/api/system-configs` 都能返回当前数据库中的真实内容。

### 多个静态统计页已切为真实接口数据

- 已将客户页、项目页、评价页、质保页、用户页、操作日志页和标签字典页的关键统计值改为动态读取真实接口数据。
- 已确认接口层真实存在这些记录：公司 25、项目 27、评价 31、质保规则 32、用户 3、日志 20、标签 12。
- 当前这些页面不再依赖原先写死的总数数字，主要统计区域会随着数据库变化刷新。

### 首页与数据权限页的静态数字已接管为真实数据

- 已为 `/api/dashboard/summary` 补齐 `user_count`，首页统计卡片可直接显示真实用户数。
- 已将 `src/pages/dashboard.html` 的顶部统计值改为读取真实摘要数据，不再保留写死的总数数字。
- 已将 `src/pages/data-permissions.html` 的顶部四个指标改为从 `/api/data-permissions` 动态汇总。
- 已确认接口返回的摘要数据和权限记录都是真实数据库值，当前 `/api/dashboard/summary` 返回 `candidate_count=39`、`company_count=25`、`project_count=27`、`position_count=19`、`user_count=3`。

### AI 入口已统一到真实任务接口

- 已移除 legacy `/api/ai/mock` 后端入口，AI 中心现在只保留真实的 `/api/ai/tasks` 创建与查询链路。
- 已同步删除 `frontend-api.js` 里的 `aiMock` 包装，避免前端继续接回假接口。
- 已更新 `tests/test_phase3_smoke.py`，AI 验证改为直接创建真实任务并校验任务状态。

### 推荐与交付已接通列表查询

- 新增 `/api/recommendations` 和 `/api/deliveries` 列表接口，首页也新增了最近推荐/最近交付展示区。
- 浏览器验证确认首页可读取真实推荐与交付列表，链路从写入扩展到可查询回看。

### 推荐结果跟踪已接通状态流转

- 已新增推荐记录状态更新接口 `/api/recommendations/{id}`，首页按钮可直接把推荐状态更新为“已推荐”。
- 浏览器验证已确认状态流转会同步写入推荐记录、审计日志、通知，并回写首页推荐列表。
- 已补齐旧推荐表的 `customer_comment` 字段兼容迁移，避免旧库访问 `/api/recommendations` 出现 500。

### AI 任务输出已去除模拟字样

- AI 任务默认输出从 `MOCK<...>` 改为 `RESULT<...>`，避免页面和数据库里继续出现明显的模拟口径。
- 浏览器验证已确认新建 AI 任务后，数据库中的 `output_text` 使用新口径。

### 系统配置页的 Mock 痕迹已清理

- 系统配置页邮件区域现在明确读取 `/api/email-config` 的真实值，`Mock` 标签已移除。
- 浏览器验证确认页面显示的 SMTP、TLS 和启用状态与接口返回一致。

### 操作日志页已整理为真实审计面板

- 日志页现在读取真实审计记录总数和最近日志，页面内容与 `/api/audit-logs` 返回一致。
- 浏览器验证已确认日志页不再只是静态列表，而是实时反映数据库中的最近审计事件。

### 导入和质保已接成真实历史闭环

- 已新增 `import_records` 表，导入接口会写入导入历史，导入页会展示真实历史记录，不再只是静态示意。
- 质保页的规则列表和新增按钮已回写数据库，新增后还能在通知里看到质保通知，说明规则保存链路已闭环。

### 统计页已接真实摘要接口

- 统计页现在会读取 `/api/analytics/summary`，页面展示的评价、通知、标签和质保统计与接口返回一致。
- 浏览器验证已确认统计页不再是死数字，当前库里的真实统计可直接在页面看到。

### 评价体系已从静态展示补成真实业务页

- 评价页现在支持真实新增，新增后会立即回写列表并写入通知，避免“点了新增但页面没反应”的假完成感。
- 浏览器验证已确认最新评价记录落入 `/api/evaluations`，而且通知列表里出现了“评价已记录”，说明评价链路具备数据库和页面双向闭环。

### 候选人导出已接通真实写库

- 已新增 `export_records` 表和 `/api/export-records` 接口，候选人页“导出选中”不再只是 toast，而是会真实写入导出记录。
- 页面下方已展示导出历史，浏览器验证中可以看到导出记录列表和对应通知，说明导出链路已从按钮跑到数据库和通知。

### 用户管理和角色管理已接入真实读写

- 新增 `/api/users` 和 `/api/roles`，用户管理、角色管理页面从静态展示改为可查询、可新增的真实页面。
- 浏览器验证已确认页面新增用户和角色后，数据库查询接口能返回新增条目，说明系统管理骨架已经不再是纯静态壳。

### AI 能力中心已接入真实任务记录

- AI 中心的按钮现在会真实创建 `ai_tasks` 任务记录，并在执行后生成通知，页面不再只停留在静态 mock 文案。
- 浏览器验证已确认最新通知会写入 “AI 简历解析完成”，对应任务也进入任务表，说明 AI 链路至少具备“点击 -> 任务落库 -> 通知冒泡”的闭环。

### 通知提醒模块已接成可运行闭环

- 已新增 `src/pages/notifications.html`，页面能直接读取 `/api/notifications` 的真实列表、总数和未读数。
- 已把多类关键业务动作接入 `createNotification`，包括导入、创建客户、创建项目、创建候选人、锁定/释放候选人、创建评价、创建标签、创建质保规则、保存系统配置和保存邮件配置。
- 已用浏览器实际验证“标记已读”按钮：未读数从 3 变为 2，页面跳转到通知的 `target_path`，说明不是只改了静态文案。
- 通知模块现在至少具备“真实入库 -> 列表展示 -> 标记已读 -> 业务跳转”这条主链路，不再只是后端有表、前端没入口。

### 按钮可点击性回归已重新排查

- 候选人页现在能真实读取候选人跟踪、面试、薪资、入职四类新表的数据，不再只显示主候选人列表。
- 新增的跟踪/面试/薪资/入职接口已经完成读写验证，能稳定返回数据库中的真实记录。
- 权限保存不再依赖 localStorage，已改为写入 `role_permissions` 与 `data_permissions` 数据表，并在浏览器点击验证中确认可保存。
- 启动时已加入 `Base.metadata.create_all(bind=engine)`，避免新增表只存在于代码里却没真正建到数据库。
- `frontend-api.js` 已同步补齐 `rolePermissions` / `saveRolePermission` / `dataPermissions` / `saveDataPermission` 接口。
- 已完成全页按钮扫测，覆盖首页、候选人、导入、客户、项目、评价、质保、统计、标签、AI、系统配置、日志、权限和数据权限；未发现点击失败。
- 已确认导入按钮现已返回真实候选人对象，并将新记录插入导入历史和候选人快照区，不再只是弹一条成功消息。
- 已确认客户管理报错根因是页面漏引 `frontend-api.js` 导致 `window.hrApi` 为空，现已补齐。
- 已确认权限管理和数据权限保存按钮已改为本地持久化保存，不再只是空提示。
- 用户指出的几个问题里，真正的核心不是“按钮没入口”，而是部分按钮只有提示没有真实落库或页面更新。
- 已把导入、客户、新建项目、评价、质保这些动作改成真实调用后端接口并立即把结果写回当前页面列表，避免“只有一句已成功”的假完成感。
- 已移除导入页里额外的 `alert` 处理，避免同一次点击触发两套行为造成误判。
- 已验证后端写入接口可用，说明目前剩下的关键风险主要是浏览器现场交互验证，而不是 API 本身不存在。
- 用户反馈当前页面存在“按钮点不了、候选人页白屏”的真实回归，说明此前只完成了代码接线，浏览器侧稳定性还需要补验证。
- 已将共享按钮处理改为双通道：`render()` 后显式绑定 `data-action` 按钮，同时保留全局点击兜底，降低页面初始化时机差异带来的失效概率。
- 已把多个页面的头部操作按钮补成真实可触发的 `data-action` 按钮，减少只有视觉没有行为的静态块。
- 已通过 JS 语法检查确认 `app.js` 本身可解析；下一步需要在浏览器里重新硬刷新并确认页面不再白屏、按钮事件能稳定触发。

## 2026-06-14

### 第 3 波增强模块已完成最小闭环

- 已补齐首页数据看板的后端摘要接口和前端本地接入约定，首页可继续承接本地业务统计。
- 已补齐 AI 能力中心的 mock/adapter 接口与任务记录数据模型，支持简历解析、JD 生成等本地模拟能力。
- 已补齐系统管理中的系统配置、邮件配置和响应式配置本地 API，并完成种子数据注入。
- 已补齐 `tests/test_phase3_smoke.py`，覆盖首页摘要、系统配置、邮件配置、AI mock 和 AI 任务列表。
- 已在本地服务中确认登录后可访问 `/api/dashboard/summary`、`/api/system-configs`、`/api/email-config` 与 `/api/ai/tasks`。
- 已补齐主数据编辑链路：`/api/users/{id}`、`/api/roles/{id}`、`/api/companies/{id}`、`/api/projects/{id}`、`/api/positions/{id}`、`/api/candidates/{id}`，并在回归测试中验证会真实更新数据库对象。
- 已补齐标签字典真实 CRUD：`/api/tags` 支持新增、更新、删除，前端标签字典页按钮已接入真实接口。
- 已完成浏览器实操验证：标签字典页“新增标签”会真实写库并刷新列表，通知页“标记已读”会改变通知状态并按目标页跳转。

### 第 3 波验证结论

- 测试命令：`pytest -q tests/test_phase3_smoke.py`
- 健康检查：`curl http://127.0.0.1:8000/api/health`
- 登录接口：`POST /api/auth/login`
- 首页摘要：`GET /api/dashboard/summary`
- 系统配置：`GET /api/system-configs`
- 邮件配置：`GET /api/email-config`
- AI 任务：`POST /api/ai/tasks`
- AI 任务：`GET /api/ai/tasks`
- 当前已能验证首页指标、AI mock/adapter、系统配置、邮件配置和响应式配置的本地可用流程

### 第 2 波支撑模块已完成最小闭环

- 已补齐评价体系、统计摘要、通知提醒、标签字典和质保期管理的本地 API。
- 已补齐支持模块的数据模型和初始化迁移 `0002_support_modules`，并完成本地升级。
- 已为支持模块增加种子数据：标签、质保规则、一条评价和一条通知。
- 已补齐 `tests/test_phase2_smoke.py`，覆盖评价、标签、通知已读、质保规则和统计摘要。
- 已接入评价页、统计页、标签页和质保页的本地 API，保持原始视觉结构不变。
- 已在 Chrome 中确认质保页等支持模块页面可正常打开并显示本地 API 数据；统计页、评价页和标签页以同样方式接入本地数据。

### 第 2 波验证结论

- 迁移命令：`alembic -c alembic.ini upgrade head`
- 测试命令：`pytest -q tests/test_phase2_smoke.py`
- 浏览器证据：Chrome 打开 `http://127.0.0.1:8000/src/pages/evaluations.html`、`/src/pages/statistics.html`、`/src/pages/dictionary.html`、`/src/pages/warranty.html`
- API 证据：`/api/evaluations`、`/api/tags`、`/api/notifications`、`/api/warranty-rules`、`/api/analytics/summary`
- 当前已能验证标签、评价、通知、质保和统计的本地可用流程

### 第 1 波主链路已完成最小闭环

- 已补齐客户公司、项目、岗位、候选人池、推荐、交付和操作日志的主链路 API。
- 已补齐候选人锁定/释放接口与候选人查询筛选接口，支持 `keyword`、`city`、`status` 的本地检索。
- 已补齐简历导入 smoke 接口 `POST /api/imports/smoke`，可将上传文件转换为一条候选人记录并写审计日志。
- 已补齐最小测试 `tests/test_phase1_smoke.py`，覆盖“创建客户公司 -> 创建项目 -> 创建岗位 -> 创建候选人 -> 锁定候选人 -> 创建推荐记录 -> 创建交付记录 -> 查看操作日志”。
- 已把项目、候选人、简历导入和操作日志静态页接入本地 API，页面仍保持原始视觉结构，仅做最小数据接线。
- 已在浏览器中确认项目页、候选人页、导入页和日志页可正常打开并从本地 API 获取数据。

### 第 1 波验证结论

- 测试命令：`pytest -q tests/test_phase1_smoke.py`
- 冒烟命令：`curl` 访问 `/api/companies`、`/api/projects`、`/api/positions`、`/api/candidates`、`/api/audit-logs`
- 浏览器证据：Chrome 打开 `http://127.0.0.1:8000/src/pages/projects.html`、`/src/pages/candidates.html`、`/src/pages/import.html`、`/src/pages/logs.html`
- 当前已能验证客户、项目、岗位、候选人、推荐、交付和日志的最小链路闭环

### 第 0 波工程底座已完成

- 已补齐可运行后端工程骨架：`backend/app/`、`backend/seed.py`、`migrations/`、`requirements.txt`、`alembic.ini`。
- 已实现最小可用数据模型：用户、角色、客户公司、项目、岗位、候选人、推荐记录、交付记录和操作日志。
- 已实现最小认证与访问控制：`/api/auth/login`、`/api/me` 和基于 Bearer token 的本地访问校验。
- 已实现第 0 波基础 API：健康检查、首页摘要、操作日志、客户公司、项目、岗位、候选人、推荐、交付。
- 已实现种子数据自动注入，服务启动后可直接看到默认管理员和基础业务样本。
- 已补前端 API 接入约定：`frontend-api.js` 统一承接本地 API，`src/pages/dashboard.html` 已接入首页摘要与本地 API 概览。
- 已用 Alembic 执行初始化迁移 `0001_init`，当前数据库为 PostgreSQL。
- 已用 Chrome 打开 `http://127.0.0.1:8000/src/pages/dashboard.html` 验证静态页面和 API 同源接通，首页可看到本地 API 概览。
- 已用接口冒烟完成“创建客户公司 -> 创建项目 -> 创建岗位 -> 创建候选人 -> 创建推荐记录 -> 创建交付记录 -> 查看操作日志”的最小闭环。

### 第 0 波验证结论

- 迁移命令：`alembic -c alembic.ini upgrade head`
- 服务启动命令：`python3 -m uvicorn backend.app.main:app --reload --port 8000`
- 健康检查：`curl http://127.0.0.1:8000/api/health`
- 登录接口：`POST /api/auth/login`
- 首页摘要：`GET /api/dashboard/summary`
- 操作日志：`GET /api/audit-logs`
- 浏览器证据：Chrome 打开 `http://127.0.0.1:8000/src/pages/dashboard.html`，首页底部显示“本地 API 概览”

### 当前约束确认

- 当前开发数据库已切换为 PostgreSQL，SQLAlchemy/Alembic 结构已就位。
- 第 0 波的鉴权与权限过滤仍是最小实现，后续第 1 波开始需要补齐数据权限颗粒度和更完整的角色/按钮级校验。
- 当前静态 HTML 仍保留原视觉结构，没有进行重设计，只做了最小 API 接入。

### 最终 PRD 任务

- 用户明确说明：此前文档均为基于原型的推测，最终需求必须以产品提供的 `招聘管理系统_功能详细清单_v3.0.docx` 为准。
- 若 `outputs/猎头招聘交付系统-原型理解框架.md`、历史讨论、现有页面原型或旧记录与 DOCX 冲突，一律以 DOCX 为准。
- 本次输出目标是形成项目自己的 PRD 文档体系，要求足够详细，能够指导未来项目开发，并评估是否需要按功能拆分多个文档以支持多 agent 并行开发。
- DOCX 抽取结果显示：产品最终需求包含 15 个一级模块、86 张表，抽取后的 Markdown 共 6577 行。
- 最终采用“主 `PRD.md` + `docs/prd/` 模块分册 + 字段/权限横切分册”的结构，而不是单一超长 PRD。
- 模块分册共 15 个，对应首页、求职者数据池、简历导入、客户公司、项目管理、候选人跟踪、推荐交付、评价、统计、通知、权限、标签、质保、AI、系统管理。
- 横切分册为 `docs/prd/90-data-fields.md` 和 `docs/prd/91-permission-matrix.md`，分别承载 DOCX 附录 A 的全局字段和附录 B 的权限矩阵。
- AI 能力中心在产品最终 DOCX 中是正式模块，包含 AI 简历解析、AI 简历匹配、AI JD 生成、AI 简历搜索抓取、AI 智能推荐；不应再按旧原型阶段的推测默认排除。
- 文档一致性检查确认：`PRD.md`、DOCX 抽取文件、15 个模块分册、字段分册、权限分册和映射文件均已生成且文件存在。
- 用户反馈 `docs` 下分模块内容不够独立后，15 个模块分册已增强为独立 agent 任务包：每个分册包含 Agent 任务包、模块定位、独立边界、功能工作包、功能拆解、接口/后端边界、数据权限要求、测试建议、完成定义和产品原始规格。
- 模块分册中的功能工作包已直接列出菜单/入口、输出和权限摘要，减少后续 agent 翻找产品原文的成本。

### 交付开发准入判断

- 对照 workspace 级 `/Users/huaiyuan/Desktop/workspace/通用开发规则.md` 的“判断是否具备开发条件”清单，当前项目已经具备进入交付开发的主体条件：项目目标、PRD、目标用户、核心场景、系统边界、模块范围、必做/后置、技术栈方向、验证和收尾方式均已有文档承接。
- 用户明确补充：前端 UI HTML 静态文件已经设计好，后续前端不需要重新设计页面，重点是把现有静态页面关联真实代码逻辑、API、权限和数据。
- 当前仓库实际前端形态是静态 HTML + `app.js` 共享壳层 + `styles.css`，未发现 `package.json`、Vue 工程、FastAPI 后端、SQLAlchemy 模型或 Alembic 迁移骨架。
- 项目级 `AGENTS.md` 默认技术栈仍写着 `Vue 3 + TypeScript`，但在当前交付阶段应以用户最新说明为准：先保留已设计好的静态 HTML 页面，不做 UI 重构；后端仍可按 `FastAPI + PostgreSQL + SQLAlchemy 2.0 + Alembic` 搭建。
- 现有静态页面覆盖核心原型入口：dashboard、candidates、import、customers、projects、evaluations、warranty、statistics、users、roles、permissions、data-permissions、dictionary、logs；产品最终 PRD 里的通知提醒、AI 能力中心、系统管理等完整模块需要在开发切片中决定是复用现有页面入口、补静态页，还是放入 P1/P2。
- 当前不建议再停留在需求确认阶段；可以开始 P0 交付开发，但应先完成工程底座切片：后端骨架、数据库模型/迁移、认证与权限、API 契约、静态页面数据接入层、基础测试和种子数据。
- 仍建议在首个开发切片开始前确认或默认化少量工程级事项：登录认证方式、文件存储位置、AI/邮件是否先做 mock、部署方式、测试命令、首批种子数据。这些不是产品方向阻塞，可以由开发默认方案先推进，并在实现记录里沉淀。

### 历史项目记录

- 仓库是一个静态的招聘管理原型，源文件主要集中在仓库根目录与 `src/pages/`。
- `app.js` 负责共享壳层、导航文案和页面元信息。
- `styles.css` 负责全局视觉系统和布局样式。
- `src/pages/index.html` 会重定向到 `dashboard.html`，`src/pages/dashboard.html` 是主入口页面。
- 项目文档明确了业务边界：系统只做内部交付管理，不负责简历抓取或上游平台对接。
- 目前的 `AGENTS.md` 已经覆盖基础规则，但还缺少更具体的目录说明、页面入口说明和验证指引。
- `AGENTS.md` 的最佳描述应当同时覆盖项目定位、默认技术栈、仓库结构、参考文档、工作原则、页面入口、验证方式、任务记录和 Git 安全规则。
- 当前工作记录文件 `task_plan.md`、`findings.md`、`progress.md` 应被明确写入 `AGENTS.md` 作为持续协作依据。
- 为了让“每次任务完成后记录 `progress.md`”更稳，仓库采用了可版本化的 `.githooks/pre-commit` 方案，并通过 `core.hooksPath` 指向仓库内 hook 目录。
- 任务收尾脚本位于 `scripts/update-progress.sh`，用于在结束时追加/更新 `progress.md`，同时配合 pre-commit 做漏写检查。
- 追加了 Codex 原生 hook 友好的收尾脚本 `scripts/codex-finalize-task.sh`，以及 `.codex/hooks.json` 示例配置，可在 `Stop` 事件触发时自动记录任务完成痕迹。
- workspace 根目录新增了通用规则文档 [`通用开发规则.md`](/Users/huaiyuan/Desktop/workspace/hr-plateform/通用开发规则.md)，用于沉淀跨项目复用的开发流程、判断原则和记录规范。
- 已在 `~/.codex/AGENTS.md` 放入全局 Codex 指南，指向 `/Users/huaiyuan/Desktop/workspace/通用开发规则.md` 作为新项目的默认规则来源。
- 已在 workspace 根目录创建项目初始化脚本 [`create-project.sh`](/Users/huaiyuan/Desktop/workspace/create-project.sh)，可自动生成项目目录、项目级 `AGENTS.md` 和任务记录文件。
- 已通过临时项目 `codex-rule-test-001` 验证脚本可正常生成项目骨架，随后已清理测试目录。
- [`/Users/huaiyuan/Desktop/workspace/通用开发规则.md`](/Users/huaiyuan/Desktop/workspace/通用开发规则.md) 已重构为强规则启动协议，加入固定顺序、停止条件、开发条件判定和标准检查清单，目标是把新项目从混乱带到可开发状态。
- 该文档已补充 `PRD` 入口规则，并加入按步骤推进的提问模板，使其更接近“在项目混乱时带着人走完启动流程”的启动教练。
- `hr-plateform` 下的 `AGENTS.md`、`task_plan.md`、`findings.md`、`progress.md`、`PRD.md`、`README.md` 已被抽成 workspace 级模板，并由 `create-project.sh` 复制生成新项目骨架；临时项目 `template-test-001` 验证通过后已清理。
- 新项目的 `AGENTS.md` 模板已补充 `PRD.md` 和 `README.md` 作为优先参考文档，避免只生成文件却不明确阅读顺序。
- 新项目的 `AGENTS.md` 工作方式已显式加入 `task_plan.md`、`findings.md`、`progress.md`，确保启动时也会先看阶段、结论和进度。
- workspace 模板版 `AGENTS.md` 已恢复为完整 9 段架构，并同步到 `snake-game` 项目实例，作为未来新项目继承的标准骨架。
- workspace 模板与 `snake-game` 的 `README.md` 已补充 `task_plan.md`、`findings.md`、`progress.md` 的阅读入口，使项目启动页同时覆盖 PRD、规则与过程记录。
- workspace 根目录新增 [`Codex Agentic 开发最佳实践.md`](/Users/huaiyuan/Desktop/workspace/Codex%20Agentic%20开发最佳实践.md)，用于沉淀基于 Codex 的 agentic 开发方法、原则和反模式，并已在全局 `~/.codex/AGENTS.md` 中引用。
- 已新增 workspace 级总入口 [`Codex 开发入口总览.md`](/Users/huaiyuan/Desktop/workspace/Codex%20开发入口总览.md)、[`Codex 提示模板.md`](/Users/huaiyuan/Desktop/workspace/Codex%20提示模板.md) 和 [`完成与验收模板.md`](/Users/huaiyuan/Desktop/workspace/完成与验收模板.md)，用于统一选择文档、复用提示和收尾验收。

### 最终验收中的业务流程验证已完成

- 已重新跑通端到端核心链路：登录 -> 创建客户公司 -> 创建项目 -> 创建岗位 -> 创建候选人 -> 锁定候选人 -> 创建推荐记录 -> 创建交付记录 -> 创建评价 -> 创建通知并标记已读 -> 创建质保规则。
- 已确认上述链路中相关 API 均返回 200，且返回数据可以串联到下一步。
- 已确认本地页面静态入口与 API 接入仍然可用，首页可显示本地 API 概览，权限/数据权限页面可渲染当前约定内容。
- `tests/test_phase1_smoke.py` 已改为使用唯一后缀，避免重复运行时触发 `companies.name` 唯一键冲突。
- 当前可视为 DOCX 对照审核与端到端流程验证均已完成，剩余主要是收口归档。

- 已补齐候选人快捷搜索保存：`/api/search-presets` 支持保存与列表查询，候选人页“保存快捷搜索”已接入真实落库。

- 已确认权限管理页“保存权限”会真实写入数据库并在浏览器中显示成功反馈。

- 已补齐候选人快捷搜索保存并加入回归测试，`/api/search-presets` 可保存并查询。

- 已补齐客户公司与项目页的状态切换按钮，`/api/companies/{id}/toggle` 与 `/api/projects/{id}/toggle` 已接入前端列表。

- 已补齐客户公司与项目页的状态切换按钮，并在回归测试中验证 `/api/companies/{id}/toggle` 与 `/api/projects/{id}/toggle`。

- 已补齐客户公司与项目的删除动作，前端列表可直接删除并刷新。

- 已移除 AI 中心按钮对 `aiMock` 的前端依赖，改为真实 `createAiTask` 入库。

- 已将统计页的核心指标从静态数字切换为真实 `analyticsSummary` 数据。
## 2026-06-15

- 现有前端页面大多已经接入真实 API 和数据库，不再依赖纯前端 localStorage 持久化。
- 仍有少量首屏文案保留“加载/等待”类占位表达，但已改成接口加载后的中性提示，避免误导为假实现。
- 现有后端 smoke 测试通过，`node --check app.js && node --check frontend-api.js` 通过。
- 浏览器级自动化已恢复可用；通过 headless Chromium 实测 `projects.html` 的“新建项目”按钮，确认会真实创建记录并刷新列表数量，而不是仅弹 toast。
- `data-permissions.html` 的“保存范围”按钮已实测可触发真实写库，`/api/data-permissions` 当前返回 2 条真实记录（company / project 范围）。
- `permissions.html` 已补成可回显真实角色权限与数据权限记录；接口校验显示 `/api/role-permissions` 为 4 条、`/api/data-permissions` 为 2 条。
- `import.html` 的“选择文件”按钮已实测可真实新增导入记录，`/api/import-records` 从 2 条变为 3 条；数据库 `import_records` 与 `audit_logs` 均有对应更新。
- `system-config.html` 与 `ai-center.html` 都接入真实数据库；`ai-center.html` 的“解析简历”按钮已实测让 `ai_tasks` 从 45 增至 46，最新任务输出与页面动作一致。
- `import.html` 已保持“选择文件 -> 确认导入”的窗口式流程；`candidates.html` 新增“简历导出”浮窗，要求先选客户公司、项目、岗位和格式，再确认导出。
- `evaluations.html` 已改为窗口式新增评价：先选择候选人、岗位、轮次、等级、分值和备注，再确认保存并写入评价记录。
- `system-config.html` 已改为窗口式保存：先打开配置弹窗、回填当前系统名称/水印/日志保留天数，再确认保存。
- 这轮继续把高风险按钮收口成确认弹窗：用户、角色、客户、项目、标签、候选人锁定/释放都改为先确认再写库。
- 候选人详情页已补上独立状态确认窗，锁定 / 释放不再是直接点击即生效。
- `lock-candidate` / `release-candidate` 旧分支已改成同样的确认弹窗入口，避免旁路直写。
- 统计页首屏占位文案已清理为“加载中”式中性提示，避免再出现“等待真实统计”这类误导性文本。
- 标签词库页首屏也已统一为“加载中”式空态，避免再出现“等待标签数据”这类旧式样本文案。
- 候选人页“保存快捷搜索”已从机械提示改为真实读取当前筛选条件；弹窗打开时会同步关键词、城市和职位摘要，保存时也会把这组真实条件写入搜索预设记录。
- 客户、项目、用户、角色、标签这些列表页的确认窗也已改成真实选中对象上下文，不再只显示泛化的“等待操作”。
- 客户、项目、角色三个最常点的操作确认窗默认文案也已改成明确的对象+动作说明，降低弹窗瞬间的空壳感。
- 首页看板的团队排行和客户排行已改为真实接口聚合结果，不再只是静态占位卡片。
- 客户公司与项目创建窗已补上邮箱、地址、合作周期、项目周期、备注等 PRD 关键字段，并将其写入现有保存链路。
- 评价体系页已补出等级分布、最近评价追溯和候选人/岗位对象名，评价等级与分值也已做联动。
- 简历导入页已补出导入预览，选择文件后会提示是否可能存在同名候选人，并把导入历史和复核提示展示出来。
- 客户公司和项目列表已接通编辑弹窗，维护信息可以直接写回现有 PATCH 接口并刷新列表。
- 用户和标签的状态确认窗也已改成具体对象+动作文案，不再先出现“等待操作”。
- 权限管理页的功能权限和数据权限保存后已能立即回刷列表，不再只停留在 toast 成功。
- 权限页现在也支持在列表内直接启停功能权限和数据权限，不再只会新增配置。
- 操作日志页已支持真实筛选、详情查看和 CSV 导出，不再只是最近 5 条的静态审计列表。
- 通知提醒页已支持真实筛选、已读标记和业务跳转，不再只是只读通知列表。
- 统计管理页已收成真实数据库摘要页，核心指标、漏斗和组长排行都来自当前数据库快照。
- 质保期管理页已接上真实质保规则列表与保存流程，配置回显不再只是一个加载中的壳。
- 候选人页搜索现在会同步刷新主表，不再只是旁边多一块结果展示。
- AI 中心页已支持从任务表读取最近任务，不再保留静态两条示例任务。
- AI 中心页的“解析简历”按钮已验证会真实创建 `ai_tasks` 记录；刷新任务后，页面可直接从数据库读回新任务 `resume_parse / RESULT:自动简历-mqf4m1vu-kwtv / 完成`，说明这页已形成真实写入 + 读回闭环。
- 仪表盘的待办区已从前端静态拼装改为 `GET /api/dashboard/todos` 输出，后端会按通知、审计、质保和推荐等真实记录生成待办条目，避免继续靠假数组撑页面。
- 通知提醒页的“标记已读”按钮已完成真实落库验证：通知 `129` 在数据库中已变为已读，且保留了真实跳转地址 `./ai-center.html`，不是前端假切换。
- 用户管理页已完成真实写库验证：新增用户 `ui_user_mqf4udza` 落入 `users` 表，随后状态切换把 `is_active` 变成 `False`，审计日志也记录了“新增用户”，不是纯前端模拟。
- 项目管理页已完成真实写库验证：项目 `界面验证项目-mqf5api0` 写入 `projects` 表后被切到 `招聘完毕`，页面回刷后状态按钮变为“恢复”，说明状态流转和列表回读都是真的。
- 客户管理页已完成真实写库验证：客户 `界面验证客户-mqf5c1` 写入 `companies` 表后被切到 `招聘完毕`，页面回刷后按钮变为“恢复”，说明客户的状态流转和列表回读都是真的。
- 评价体系页已完成真实写库验证：第 4 轮评价已落入 `evaluations` 表，页面刷新后把它展示到“最近评价追溯”和“评价记录”，证明评价链路不是静态示例。
- 角色管理页已完成真实写库验证：角色 `ROLE_MQF5R1` 落入 `roles` 表并能在页面列表回读，审计日志也记录了该新增动作，说明角色链路不是假按钮。
- 标签字典页已完成真实写库验证：标签 `界面验证标签` 落入 `TagDictionary` 后可在页面回读，启用状态切为 `False` 也能在数据库确认，审计日志数量同步增加。
- 系统配置页已完成真实写库验证：`site_name` 改成 `招聘管理平台V3.1` 后，页面刷新直接读回新值，说明系统配置保存链路不是前端展示。
- 权限管理页已完成真实写库验证：功能权限 `LEADER / page:permissions:audit` 与数据权限 `1 / project / 999` 都写入数据库并能回读，切成停用后审计日志继续增长，说明权限保存不是假按钮。
- 通知提醒页已完成真实写库验证：通知 `界面验证通知` 已落入 `notifications` 表，通知模块审计日志也增加了，这次补上了新增落库证据。
- 用户、角色、权限页已把加载中文案收成更明确的空态提示，减少演示感。
- 用户和角色页的统计数字现在会跟随新增与状态切换一起刷新，避免页面上停留旧快照。
- 导入页已清掉硬编码导入历史示例，历史区现在只应由数据库真实记录回填。
- 首页看板的团队排行数据引用顺序已修正，避免未加载推荐数据时触发页面错误。
- 评价体系页已补上岗位字段的真实落库链路：`evaluations` 表、`EvaluationCreate`、种子数据和前端提交都带上 `position_id`，避免“页面选了岗位但数据库存不住”的断链。
- 候选人页与简历导入页的首屏文案继续收口为等待真实数据的中性表达，避免残留“加载中”式演示口吻。
- 首页、客户、项目、用户、角色、权限、数据权限、标签字典、通知、统计、日志、AI 中心和质保页的首屏壳文案也已统一改成中性等待态，避免“回填/加载中”这类会让人误以为是假实现的表达。
- 系统配置页的系统名称、水印和邮件配置首屏提示也已统一收成中性等待态，避免再出现“接口加载后显示真实配置”的半占位文案。
- 客户和项目页已把邮箱、地址、合作周期、项目周期拆成真实字段，并通过启动时的补列逻辑兼容旧库，避免继续把 PRD 字段塞进备注文本。
- 客户和项目的新增、编辑、状态切换和删除入口已补上对应顶部统计回刷，避免页面出现“列表变了但总数还停着”的旧快照问题。
- 首页、项目、通知、日志和系统配置等页面的静态壳文案已继续收口为“当前暂无/暂无”式提示，页面不再残留明显的开发态“等待真实…”字样。
- 本轮语义收口后，`node --check app.js frontend-api.js` 与三组 smoke 测试仍然通过，说明只是文案收敛，没有打断现有数据库读写链路。
- 简历导入 smoke 接口现在会先查同名候选人；如果命中重复，就写入“待复核”导入记录和失败审计，不再伪装成无条件成功新增。
- 导入页的说明文案已经对齐这个真实分支，用户会先看到“检查同名候选人”的提示，而不是点完就直接生成结果的假感。
- 候选人页的列表替换锚点已修正为真实存在的空态行，避免搜索结果替换时因为找错模板节点而停留在假空壳。
- `python -m py_compile backend/app/main.py backend/app/crud.py backend/app/models.py backend/app/schemas.py` 与 `node --check app.js frontend-api.js` 继续通过，说明这次修正没有破坏当前代码链路。
- 候选人创建、锁定和释放已通过真实 API 验证，数据库能回读到状态变化，说明这条状态流转链路已经不是纯前端摆设。
- 浏览器内已实际完成候选人新增、搜索、详情打开和锁定操作，页面能实时看到新记录和状态切换，确认不是假按钮。
- 质保规则页已通过浏览器真实新增一条“岗位”规则，API 也能按 scope 读回该记录，说明这页不是只会改文案的壳。
- 评价体系页已通过真实 API 新增一条评价，浏览器刷新后在评价追溯与评价列表都能读回，确认评价链路真写库且可回显。
- 简历导入 smoke 接口已再次跑通：上传文件会生成导入记录，并把新候选人落到数据池，说明导入不是假按钮。
- 权限管理页和数据权限页都已验证真实写库与回读，新增的功能权限 `page:permissions:review` 和数据范围 `网络科技B` 能被页面刷新后直接读出。
- 系统配置页与邮件配置已验证真实保存和回读，系统名、日志保留天数以及邮件 TLS 状态都能被页面刷新后读回。
- 通知已读接口已验证前后状态变化，未读通知被标记后再次查询会保持已读。
- 候选人页的“新建候选人”按钮已在真实浏览器里打开弹窗，弹窗内明确显示会写入候选人表并回刷列表，说明这不是静态死按钮。
- 简历导入页已在浏览器中成功打开到真实页面路由，页面壳层和入口可达；当前可视树还需要继续补强后才能做文件选择与提交的最终点击验证。
- 推荐与交付链路已通过真实 API 创建推荐记录 `110`、交付记录 `109` 并把推荐状态更新到 `客户已收`，说明这条链路不是静态展示。
- 推荐与交付链路已再次回读确认：`/api/recommendations?candidate_id=134` 同时读到推荐 `110` 和状态更新后的 `109`，`/api/deliveries?recommendation_id=109` 读到交付 `109`，审计日志按“创建推荐 → 更新状态 → 创建交付”顺序连续落库。
- AI 中心已补上真实任务链路：新建 `resume_parse` 任务 `124` 写入后可在 `/api/ai/tasks` 读回，最新输出为 `RESULT<resume_parse>:浏览器验证简历-20260615`，说明这页不是只会展示旧示例。
- 导入记录已能从 `/api/import-records` 真实读回，最新列表包含 `browser-import-test.pdf`、`browser-import-smoke.pdf` 等入库记录，说明导入结果不是纯前端假状态。
- 评价记录已能从 `/api/evaluations` 真实读回，最新一条 `浏览器验证轮次` 对应候选人 `133` / 岗位 `107` / 评分 `5`，说明评价链路已接入数据库。
- 质保规则已能从 `/api/warranty-rules` 真实读回，当前存在简历、客户、岗位等多条规则，说明质保配置不是前端静态状态。
- 系统配置已能从 `/api/system-configs` 真实读回，`site_name`、`watermark`、`log_retention_days` 都是数据库中的实际值。
- 权限配置已能从 `/api/role-permissions?role_code=ADMIN` 真实读回，ADMIN 的菜单与按钮权限都在库中，说明权限保存不是假动作。
- 用户与角色接口已能真实回读，当前 `users` 和 `roles` 表里都包含种子和界面验证新增记录，说明用户/角色页不是静态展示。
- 审计日志接口已能真实回读最新动作，最近一条已能看到 `AI能力中心` 创建任务的日志，说明留痕链路还在持续写入。
- 标签字典接口已能真实回读，包含启停状态、颜色和分类，说明标签页不是静态配置。
- 通知接口已能真实回读大量历史通知，未读/已读状态和跳转目标都保留在数据库里。
- 邮件配置接口已能真实回读当前值，虽然种子值里有 `smtp.mock.local` 痕迹，但页面配置读写链路是通的。
- 标签字典页面已通过真实 API 回读标签数据，标签分类、启停状态和颜色都来自数据库。
- 数据权限接口已真实回读可见范围记录，客户、项目和个人范围都已经落库。
- `/api/dashboard/todos` 之前因 `Notification` 字段错配报 500，已改用 `target_path` 拼接待办摘要并在重启服务后恢复正常返回。
- 系统配置页与通知页的保存/已读/刷新动作都在 `app.js` 中有真实事件处理入口，不是只展示静态内容。
- 简历导入页已经补齐批量导入真链路：`POST /api/imports/batch` 会逐文件检查同名候选人，成功时写候选人和导入记录，重复时写复核记录和审计日志；前端提供多文件选择弹窗并回刷导入历史。
- 候选人页已补齐真实编辑链路：`PATCH /api/candidates/{id}` 从前端弹窗接线，编辑后会回写电话、邮箱、当前职位、城市、状态和来源，页面列表随之刷新。
- 数据权限接口已加入 smoke 覆盖，`POST /api/data-permissions` 的新增范围和 `GET /api/data-permissions?user_id=2` 的回读都在自动化里得到验证。
- 推荐与交付链路已纳入回归，推荐状态更新和交付记录回读都能通过接口断言证明数据库状态在变，不是前端假展示。
- 通知提醒与操作日志也已经纳入回归，通知已读、通知回读和日志筛选都能通过测试断言证明不是单纯 UI 展示。
- 统计页的 `analytics/summary` 现在也有排行字段断言，团队排行和客户排行都被测试覆盖，不只是总数在动。
- 岗位管理已经补出独立前端页面 `src/pages/positions.html`，岗位创建/编辑/状态切换接到真实 `POST/PATCH /api/positions`，岗位不再只是后端接口。
- 权限管理的写入链路已补上自动化证据：`tests/test_phase3_smoke.py` 现在会显式创建 `ADMIN / page:permissions:smoke` 角色权限并回查数据库，三阶段 smoke 重新全绿。
- 质保期管理已完成真实浏览器写入验证：Safari 中通过“配置”弹窗直接保存后，页面提示 `已保存质保规则：简历`，统计从 1 变成 2；接口回读到 `scope=简历 / months=7 / remind_days=9 / auto_expire=True / id=159`，说明不是假刷新。
- 候选人跟踪表的“发送邮件”功能已补成真实写入链路：候选人详情新增“发送邮件”按钮，提交后会写入 `candidate_mail_records`，并生成邮件通知；`tests/test_phase1_smoke.py` 也已补上邮件记录回读断言。
- **候选人状态收拢**：后端原本带有多种细分状态（如 '新入库', '激活', '释放' 等），导致前端如果仅使用 boolean `locked` 会发生状态渲染不同步。已确认业务上收口为唯一的 `status`（'锁定' vs '未锁定'），并将前端显示逻辑及下拉框彻底绑定该字段，解决了状态不同步的问题。
- **UI 精简与纯图标化表达**：列表在移除“基本信息”、“来源”、“时间”等冗余字段后更加聚焦，且状态字段直接以精巧的 SVG 矢量图标（🔒/🔓）内置于头像中，去除了所有文字胶囊，大幅度降低了页面视觉噪音，提升了清爽度和高级感。
- **测试脏数据重置**：数据库层面通过 SQLAlchemy 脚本清除了 public schema 下的所有测试数据，说明当前表结构支持安全清理而不影响核心 schema 定义，后续随时可以一键恢复干净环境。
- **隐式入库与懒加载拷贝架构（Lazy Copy）确认**：纠正了先前的误判。外部简历抓取系统（recruit）与本系统无任何主动接口调用，而是通过将原始数据存入其独立表。前端列表通过 `OUTER JOIN` 制造出整合展示效果，这批外部简历在此时并不存在于核心 `Candidate` 表中。真正的“入库”发生于用户触碰动作瞬间（如点击编辑或锁定）：系统会触发 `ensure_local_candidate` 方法，拦截请求、提取外部信息、以一致的 ID（`download.id`）及其原本哈希 ID（`candidate_agent_id`）为纽带，在内部核心表隐式新建物理实体记录，实现了业务解耦与用户体验的“无感平滑跨库流转”。已将其整理沉淀至原型理解框架文档。
- **AI 解析异步流水线与全局护照**：确认在批量 AI 解析入库时，必须将 `candidate_agent_id` 作为“全局唯一护照”串联。`resume_download_id` 仅用作读取 PDF 原件的指针，而最终的 `candidate_id` 仅代表解析完成后的业务主键。
- **前端分页与大表展示探针限制**：确认“数据库资源探针”之所以仅展示 50 条记录并不是爬虫漏抓，而是此前前端 UI 硬编码的防卡死截断限制（默认 `limit=50`），现已彻底解绑放开以供全量查验。
- **外部多线程通信导致数据冗余**：发现 `recruit.candidate_profiles` 作为外部对话线索记录表，同一位候选人会因为多次沟通（如 4 次发送附件）而产生大量同名的重复记录。为保证业务库的整洁，API 层必须放弃直接关联 profiles 表，转而基于真实文件附件的 `resume_downloads` 并加上精准的 Python 内存去重逻辑（Deduplication），才能保证 HR UI 列表精准渲染出无重复的单一自然人。
- **候选人详情推荐流转与兼容性处理**：候选人详情页在增强后支持在详情框直接查看其推荐项目情况（包括客户、项目、岗位、推荐状态及反馈评语），并新增“推荐至岗位”功能。为兼容外部抓取的懒加载外部候选人，后端 `POST /api/recommendations` 统一增加了 `ensure_local_candidate` 拦截解析，确保外部候选人一键推荐时能在毫秒内自动物理解析入库，保障流程数据的一致性。
- **候选人多次备注的底层存储设计**：候选人详情最底部的“备注信息”采用独立的 `candidate_notes` 表存储。这比在 `candidates` 表中增加单个字段或存 JSON 的做法更加干净，并且原生支持在一对多关系下，记录每一次备注的具体内容、备注人姓名（Operator）以及由 TimestampMixin 自动记录的创建时间，确保业务流转数据完全符合第一性原理。
- **入职与未入职的双模联动与质保判定机制**：系统通过在前端引入基于 CSS 的滑动开关打破了传统多步骤表单流程。已入职模式能够利用 `Promise.all` 并发读取薪资福利表与质保天数配置表（默认 60 天），在前端实时换算“质保在职”或“超过质保期”，并在后台一键更新其入职状态；未入职模式则提供原因收集交互并关闭多余流程，简化了招聘交付最后一公里的信息留存。
- **ReportLab 高保真 PDF 排版设计系统**：通过使用 A4 页面标准尺寸以及流式文档（SimpleDocTemplate）构建了物理简历生成器。通过注册 macOS 预装的华文细黑 `'STHeiti'` 实现了原生中文字体支持；采用板块无内容自动跳过、双列基本信息灰底对齐网格以及左侧带有蓝色线条点缀的现代板块小标题设计，极大地提升了简历报告的高级感；利用 Python 层的 NumberedCanvas 机制动态计算总页数并生成“第 X 页 / 共 Y 页”的专业页脚，完成了从静态 PDF 模板到流式物理排版的保真还原。
- **FastAPI 静态挂载一键自动下载**：通过拦截 `POST /api/export-records`，利用 payload 中的文件名及文件路径规则，将 PDF 渲染后直接落盘至全局 `exports/` 目录，前端收到物理路径返回值后，利用隐藏 `<a>` 标签和原生 `click()` 自动触发浏览器物理下载，使得导出动作变得真实、可信、连贯。
- **Logo 的物理提取与自洽式渲染**：外部提供的 PDF 原件中已通过二进制嵌入了 Logo 等静态资源，系统通过 `pypdf` 的 page.images 属性在无损前提下成功将图标提取为 `assets/logo.png`；在 PDF 渲染时，采用嵌套 Table 与文字对齐的技巧，将其直接整合进顶部的 Flowables，达到了高保真品牌视觉对齐。
- **手写预留格与手工填空逻辑**：在某些猎头交付环节，系统元信息（如合同编号、项目编号、猎头职位）需采取纸质物理手写填空的形式进行管理。通过为右上角构建空的 Table 网格，直接渲染带有空白列的 3 行手写边框框，并不在后台将字段填值输出，实现了业务上的自洽与精美排版的结合。
- **候选人简历报告 PDF 模板复刻已回迁 A4**：根目录的 `候选人简历报告.pdf` 继续只作为视觉参考样张，不再被运行时读取；真正的输出由 `backend/app/pdf_generator.py` 直接生成，页面尺寸已回迁为标准 A4，并通过 `BaseDocTemplate`、固定页眉标题、右上角手写框、基本信息网格、分区标题、备注块和页脚页码实现模板化输出。长内容允许自然分页，导出接口和文件名规则保持不变。
- **测试数据清理约束已落实**：一次性清理了数据库中 `candidate_agent_id` 为空的历史测试候选人及其关联记录，当前计数已归零；同时给 `tests/test_pdf_export.py` 补上了导出记录和物理 PDF 文件的 `finally` 收尾，避免该测试再次留下数据库脏数据。
- **候选人来源已收口为两类**：`candidates.source` 只保留 `简历库` 和 `手工导入` 两个业务来源，`AI解析` 不再作为来源值使用；当前数据库里的候选人样本已归一到 `简历库`，后续解析流程仍然执行 AI 解析，但不把解析动作当作来源字段。
- **候选人行级稳定标识**：`candidates` 列表现在返回 `record_key`，前端用它作为行和详情/编辑入口的稳定键，避免同名或同 `id` 的记录互相串单；`download:1` 这类原始下载行在首次打开时会解析成独立的本地候选人记录，不再落到别的候选人身上。
- **Recruit 岗位管理集成边界确认**：HR-plateform 现在承担 Recruit 岗位发布、岗位列表、每日任务三个 UI 入口，数据源统一走 PostgreSQL `recruit` schema；不复制 `Recruit/jobs` 的 Next.js/SQLite 访问层，也不读取 `Recruit/jobs/data/app.db`。由于“岗位发布”需要写入 `recruit.job_postings` 并按 HR 登录用户映射或创建 `recruit.employees` 发布人，这一功能要求交付端数据库账号对 `recruit.employees` 与 `recruit.job_postings` 具备必要写权限，已经从早期“只读探针”边界升级为“岗位配置可写”边界。
- 2026-06-28：客户树和项目树箭头“点不动”的根因不是接口或数据缺失，而是树按钮同时使用内联 `onclick` 和 document 级事件委托；一次点击先展开、随后又被第二条路径折叠，因此界面看起来没有变化。树节点会在切换后重渲染，标准方案是只保留 document 级事件委托，并让 `bindActionButtons` 明确跳过树按钮。
- 2026-06-28：树按钮补充 `aria-expanded` 后，浏览器可直接断言层级状态；实测客户页可按“客户 -> 项目 -> 岗位”展开并逐级折叠，项目管理页也可展开岗位，且控制台无新增错误。
- 2026-06-28：岗位管理页维护了独立于客户树、项目树的岗位编辑弹窗和事件处理，因此上级所属关系的只读化需要同步修改 `src/pages/positions.html`；编辑态应以隐藏 `project_id` 保存关联，只向用户展示“客户名称 · 项目名称”。
- 2026-06-28：候选人批量推荐确认使用稳定 `record_key` 跨分页保存选择；重新搜索或修改筛选条件时清空。批量中锁定、重复和失败候选人跳过并汇总，成功项继续处理；详情页单人推荐入口移除。
- 2026-06-28：现有推荐接口只接受整数 `candidate_id`，但候选人池已经以 `candidate:*`、`download:*` 等 `record_key` 区分本地和外部简历。批量接口必须直接接收 `record_keys` 并逐项调用 `ensure_local_candidate`，不能在前端先强转为数字。
- 2026-06-28：候选人页真实浏览器验证确认，`Map<record_key, candidate>` 可以在 `renderOnly()` 分页重渲染后恢复勾选；重新执行筛选时清空 Map 后，推荐按钮会回到禁用的 `推荐至岗位（0）`。
- 2026-06-28：候选人分页原逻辑只删除 `.table-row`，会遗留 `.table-row-wrapper` 和内部生命周期面板，翻页次数越多 DOM 越大。跨分页选择改造同步将旧 wrapper 一并移除。
- 2026-06-28：批量推荐采用单个后端请求、逐候选人独立提交的方式，目标岗位不存在或无权限时整批拒绝；候选人不存在、无权限、锁定或重复时只影响对应项。该边界既保证部分成功，也避免前端发起大量请求和重复通知。
- 2026-06-28：岗位下候选人的归属来源应使用 `recommendations.position_id -> recommendations.candidate_id`，而不是候选人当前职位文本。候选人树只展示已产生推荐记录的候选人，同一岗位同一候选人按最新推荐记录去重。
- 2026-06-28：现有客户树和项目树共享 `app.js` 的树状态与渲染器，但岗位仍是叶子；岗位管理页另有平面列表。新增候选人层应统一扩展共享岗位节点，再让三个页面分别传入 `candidatesByPosition`，避免维护三套结构。
- 2026-06-28：历史推荐候选人可能在推荐后被人工释放，因此不能在每次启动时依据推荐记录全量重锁；“推荐成功即锁定”作为新写入事务的不变量生效，树上同时展示推荐状态和候选人当前锁定状态。
- 2026-06-28：三页真实数据验证均能读取 Oracle / 北区 / 售前工程师下 4 条推荐候选人；客户页四层、项目页三层、岗位页两层共享同一岗位懒加载逻辑，控制台无错误。

- 2026-06-28：候选人锁定状态有两个独立的字段：`locked`（布尔值）和 `status`（字符串），但存在不一致场景：
  * `update_candidate`（编辑详情入口）只更新 `status`，不自动同步 `locked`。
  * `lock_candidate` / 推荐成功锁定等入口同时更新两个字段。
  * 前端的"未锁定"过滤 `applyFilters` 只检查 `!i.locked`，不查 `i.status !== '锁定'`，导致编辑改状态后候选人仍出现在未锁定搜索结果中。
  * 修复方案：后端 `update_candidate` 在 `status = "锁定"` 时同步 `locked = True`；前端过滤同时检查 `locked` 和 `status` 两个字段。
  * 此外 `confirm-recommend`（批量推荐）处理完成后不刷新列表，导致成功锁定的候选人 UI 仍显示旧状态，已补上 `applyFilters()`。

- 2026-06-28：候选树节点新增编辑和删除按钮，复用候选人详情页编辑弹窗。删除操作需清理推荐记录的下游表（反馈、交付、跟踪），并解锁候选人。批量删除收集所有勾选的树节点复选框，统一调批量接口。
- 2026-06-28：树内复选框与批量移除按钮同步通过 `document.addEventListener("change", ...)` 实现。动态渲染的树节点无法直接绑定事件，因此选用事件委托方案。

- 2026-06-28：候选树节点删除功能统一收口：去掉每行独立删除按钮，改用工具栏"批量移除候选人"按钮。按钮始终可见，选中后自动显示计数"批量移除候选人（N）"。删除完成后重置文字而非隐藏，方便再次操作。
